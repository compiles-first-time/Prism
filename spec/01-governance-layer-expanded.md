# Spec 01 Expanded: Governance Layer

**Source:** `001/spec/01-governance-layer.md`
**Exploration:** `002-spec-expansion`
**Status:** draft — pending self-audit and back-propagation passes
**Priority:** 1 (foundation — every other spec references these SRs)
**Reserved SR range:** `SR_GOV_01` through `SR_GOV_99`
**Last updated:** 2026-04-10

---

## Purpose

Turn the Governance Layer architectural decisions from Spec 01 into implementation-ready SR rows. Every row has 12 columns populated, full exception coverage where exceptions can actually occur, explicit Next Step, and an explicit Why. Any SR can be implemented directly from this spec without ambiguity.

## Architectural Decisions Covered

D-3 (priority stack), D-17 (query analytics), D-18 (IAM integration), D-19 (rejection justification), D-20 (ENFORCE vs ADVISE), D-21 (IAM conflict resolution), D-22 (event-sourced audit with Merkle chain), D-29 (model hot-swap governance), D-34 (CSA), D-35 (employee lifecycle), D-37 (platform-managed identity), BP-26 (bulk import approval), BP-27 (schema change response), BP-28 (retention policies), BP-29 (alert routing), BP-30 (classification downgrade), BP-31 (cross-tenant learning opt-in), BP-32 (relationship confidence thresholds), BP-33 (embedding model change), BP-56 (training data consent), BP-57 (prompt template approval), BP-76 (feature flags), BP-77 (admin undo), BP-85 (component lifecycle governance), BP-86 (AI-generated code governance), BP-92 (service account governance).

## Trunk Inheritance

These trunk capabilities are INVOKED by this spec but are not themselves re-expanded (locked in trunk):

| Capability | Trunk Reference | Invoked by SRs |
|-----------|----------------|----------------|
| LCA approval algorithm | `FOUND § 1.4.1` | `SR_GOV_41`, `SR_GOV_42`, `SR_GOV_43` |
| DRPRR dual-reporting rules | `FOUND § 1.4.2` | `SR_GOV_42`, `SR_GOV_14` |
| DEF delegation framework | `FOUND § 1.4.6` | `SR_GOV_44`, `SR_GOV_45` |
| Service Principal identity | `FOUND § 1.3.1` | `SR_GOV_01`, `SR_GOV_10`, `SR_GOV_13` |
| Lifecycle state machine | `FOUND § 1.5` | `SR_GOV_01`, `SR_GOV_13` |
| Visibility Compartment | `COMP § 3.1.1` (GAP-77) | `SR_GOV_31` through `SR_GOV_36` |
| CaaS credential management | GAP-14 | `SR_GOV_10`, `SR_GOV_13`, `SR_GOV_14`, `SR_GOV_66` |
| Break-glass emergency access | GAP-49 | `SR_GOV_14`, `SR_GOV_29`, `SR_GOV_46` |
| Audit trail (event-sourced + Merkle) | GAP-71 | `SR_GOV_47`, `SR_GOV_48`, `SR_GOV_49`, `SR_GOV_50`, `SR_GOV_51` |
| Crypto-shredding | `COMP § 3.3.3` | `SR_GOV_52` |
| BSA/AML classification registry | GAP-78 | `SR_GOV_53`, `SR_GOV_58` |
| REPW regulatory examiner workflow | GAP-80 | `SR_GOV_43` (examiner approval path) |
| MLAID injection defense | GAP-61 | Governance does not expand MLAID here; invoked by `SR_LLM_` spec |
| DBE v2 verification pipeline | `ARCH § 2.3.1` | Governance does not expand DBE v2 here; invoked by `SR_LLM_` spec |
| Model collapse prevention | GAP-44 | `SR_GOV_60` (training data consent) and `SR_LLM_` expansion |
| Fairness monitoring RTFMC | GAP-59 | Referenced from decision-support expansion |

## Evidence Grades

All architectural decisions in this expansion inherit their evidence grades from 001/STATE.md and the trunk. No new grades are assigned. Where a specific threshold or algorithm is referenced, the trunk citation carries the evidence.

## Integration Map (Other Specs That Depend on SR_GOV_)

| Consumer Spec | Depends On SR_GOV_ |
|--------------|-------------------|
| Spec 02 Data Model | `SR_GOV_01`, `SR_GOV_47`, `SR_GOV_48`, `SR_GOV_53`, `SR_GOV_56` |
| Spec 03 Connection Layer | `SR_GOV_70`, `SR_GOV_76`, `SR_GOV_53`, `SR_GOV_57`, `SR_GOV_58`, `SR_GOV_03` |
| Spec 04 Intelligence Layer | `SR_GOV_33`, `SR_GOV_77`, `SR_GOV_59`, `SR_GOV_32` |
| Spec 05 LLM Routing | `SR_GOV_73`, `SR_GOV_16`, `SR_GOV_65`, `SR_GOV_66` |
| Spec 06 Decision Support | `SR_GOV_24`, `SR_GOV_74`, `SR_GOV_71`, `SR_GOV_72` |
| Spec 07 Interface | `SR_GOV_75`, `SR_GOV_68`, `SR_GOV_69`, `SR_GOV_37` |
| Spec 08 Component Catalog | `SR_GOV_61`, `SR_GOV_62`, `SR_GOV_78` |
| Spec 09 Service Account Catalog | `SR_GOV_63`, `SR_GOV_64` |
| Spec 13 Scalability | — (governance rules enforced across scaled components) |

---

## Reusable Components Referenced by SR Rows

These components are shared across many SRs and are named once here to avoid duplication:

| Component ID | Purpose |
|-------------|---------|
| `REUSABLE_AuditLogger` | Writes append-only audit_events rows with Merkle hash chaining (D-22, GAP-71) |
| `REUSABLE_TenantFilter` | Injects `tenant_id` constraint into Cypher and SQL queries (IL-1) |
| `REUSABLE_CaaSCredentialRetriever` | Fetches encrypted credential reference from CaaS, JIT activates, returns to caller, destroys on use (GAP-14) |
| `REUSABLE_MerkleChainHasher` | Computes `prev_event_hash` + `current_event_hash` = `next_event_hash` for audit chain (D-22) |
| `REUSABLE_ApprovalChainResolver` | Computes LCA approval chain for any governance action (FOUND § 1.4.1) |
| `REUSABLE_ComplianceProfileLookup` | Returns the active compliance profile (financial / healthcare / government / SMB) for a tenant; drives vertical-specific rule evaluation |
| `REUSABLE_RateLimiter` | Per-tenant, per-caller, per-action rate limiter with token bucket semantics; drives SE-04 detection |
| `REUSABLE_Alerter` | Routes alerts via severity matrix (BP-29) to email, in-app notification center, SMS, PagerDuty |
| `REUSABLE_EventBusPublisher` | Publishes governance events to the Redis Streams event bus, partitioned by tenant_id |

---

## Section 1 — Tenant and Identity (SR_GOV_01 through SR_GOV_09)

Covers: tenant onboarding, IAM provider registration, user sync (webhook and polling fallback), role mapping, IAM conflict resolution, platform-managed identity, standalone-to-IAM migration.

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_01` | --- | governance | A platform admin onboards a new tenant by submitting a TenantOnboardingRequest, creating the Tenant node, seeding default compartments/roles for the selected vertical, and writing the initial audit record. | Platform admin credentials (`REUSABLE_CaaSCredentialRetriever`), vertical template library, Neo4j + PostgreSQL write access, `REUSABLE_AuditLogger`, `REUSABLE_MerkleChainHasher` | Management API (authenticated platform admin session) or platform admin console | TenantOnboardingRequest containing tenant name, legal entity, vertical, primary contact email, billing tier, IAM provider type, data residency preference, compliance profile | Tenant node in Neo4j (with TENANT_BOUNDARY self-edge), tenant row in PostgreSQL with default compartments + roles seeded, initial activation audit event, activation token email queued | `TenantOnboardingRequest` (JSON, management schema v1) | `TenantOnboardingResult { tenant_id, status, activation_token_ref, next_steps[] }` (JSON) | If external IAM: `SR_GOV_02`. If standalone: `SR_GOV_07`. | Establishes the multi-tenant isolation boundary that every other operation depends on. Without a correctly created Tenant node and its default compartments/roles, no downstream governance check can operate. |
| `SR_GOV_01_SE-01` | SE | governance | Neo4j or PostgreSQL is unavailable during the tenant creation transaction. | Durable retry queue, `REUSABLE_Alerter` | Connection failure exception from driver or timeout > 5s | Same TenantOnboardingRequest | Retry scheduled on durable queue; platform admin notified; no partial state in either store | Same as `SR_GOV_01` | `TenantOnboardingResult { status: "retry_scheduled", retry_at, reason }` | Retry up to 3 times with exponential backoff (5s, 30s, 120s); on exhaustion, mark FAILED and alert on-call SRE. | Detected via driver exception. Both-store atomicity is required. Partial state leaks a tenant to users it is not assigned to; strictly forbidden. |
| `SR_GOV_01_SE-02` | SE | governance | The caller's authentication token expires mid-request (credential failure after initial auth). | CaaS, re-auth endpoint | Token expiration detected by Management API middleware | Same TenantOnboardingRequest + expired token | Request rejected with 401; activation not started; audit event `tenant_onboarding_auth_expired` written | Same as `SR_GOV_01` | `AuthError { code: 401, reason, retry_after }` | Caller must re-authenticate and resubmit. No retry by platform. | Prevents unauthorized onboarding via token replay and ensures each onboarding event is attributable to a valid authenticated identity. |
| `SR_GOV_01_BE-01` | BE | governance | Duplicate tenant name or legal entity identifier violates uniqueness constraint. | PostgreSQL unique index on `tenant.name` and `tenant.legal_entity_id` | Validation layer check and DB constraint exception | TenantOnboardingRequest with duplicate name | Request rejected with 409; audit event `tenant_onboarding_duplicate` written; caller instructed to choose different name | Same as `SR_GOV_01` | `ValidationError { code: 409, field, existing_tenant_id? }` | End (no retry). Caller must resubmit with unique identifiers. | Name or legal entity uniqueness guarantees that downstream identifier resolution is deterministic. |
| `SR_GOV_01_BE-02` | BE | governance | The requested vertical or compliance profile does not exist or is not supported in the caller's region. | `REUSABLE_ComplianceProfileLookup` | Profile lookup returns null or "unsupported" | TenantOnboardingRequest with invalid vertical or compliance profile | Request rejected with 400; audit event `tenant_onboarding_invalid_profile` written | Same as `SR_GOV_01` | `ValidationError { code: 400, field, supported_values[] }` | End. Caller resubmits with valid profile. | Prevents seeding a tenant with an undefined governance baseline, which would create silent non-compliance. |
| `SR_GOV_01_BE-03` | BE | governance | Caller is authenticated but lacks the `platform_admin` role required for tenant creation. | Role check against platform IAM | Authorization middleware | TenantOnboardingRequest + caller principal | Request rejected with 403; audit event `tenant_onboarding_forbidden` written; security officer alerted on repeated attempts | Same as `SR_GOV_01` | `AuthError { code: 403, required_role }` | End. Repeated attempts from same caller within 1 hour trigger session suspension per `SR_GOV_39`. | Tenant creation is the single most privileged governance action. Unauthorized attempts are treated as active probes. |
| `SR_GOV_02` | --- | governance | Register the tenant's external IAM provider by storing the provider type, issuer URL, client ID, and webhook endpoint; store the signing key in CaaS; test the webhook handshake. | `REUSABLE_CaaSCredentialRetriever`, IAM provider SDKs (Entra/Okta/Google/SCIM), `REUSABLE_AuditLogger` | Called from `SR_GOV_01` Next Step when IAM provider is non-standalone | `IamProviderRegistration { tenant_id, provider_type, issuer_url, client_id, client_secret_plaintext, webhook_endpoint, signing_key }` | Provider row in `iam_sync_state` table, client secret and signing key stored in CaaS, initial handshake test confirming webhook reachability, audit event `iam_provider_registered` | `IamProviderRegistration` (JSON) | `IamRegistrationResult { provider_ref, test_result, ready_for_sync }` | `SR_GOV_03` (first sync) | Binds the tenant's user directory source of truth to the platform. Without this, no user sync and no identity-based authorization can occur for IAM-synced tenants. |
| `SR_GOV_02_SE-01` | SE | governance | IAM provider webhook handshake fails (unreachable endpoint or TLS handshake error). | IAM provider SDKs, network stack | Handshake timeout or TLS error | Same IamProviderRegistration | Registration rolled back; CaaS secrets deleted; audit event `iam_handshake_failed` written; tenant remains in `AWAITING_IAM` state | Same | `IamRegistrationResult { status: "handshake_failed", error, remediation }` | Platform admin must correct endpoint/certificate and re-run `SR_GOV_02`. Up to 5 retries per hour before rate-limited. | Handshake failure indicates misconfiguration that must be resolved before user sync can operate reliably. |
| `SR_GOV_02_SE-02` | SE | governance | CaaS is unavailable when attempting to store the client secret and signing key. | CaaS + retry queue | CaaS client exception | Same IamProviderRegistration | Durable retry scheduled; no partial state; alert to on-call SRE | Same | `IamRegistrationResult { status: "retry_scheduled" }` | Retry with exponential backoff (10s, 60s, 300s); on exhaustion, mark tenant registration FAILED and alert security officer. | CaaS is the only authorized store for secrets; plaintext secrets must never persist outside CaaS. |
| `SR_GOV_02_BE-01` | BE | governance | `provider_type` is not one of the supported providers (Entra, Okta, Google, SCIM 2.0). | Provider type allowlist | Validation layer | IamProviderRegistration with unsupported provider | Request rejected with 400; audit event `iam_provider_unsupported` | Same | `ValidationError { code: 400, supported: [Entra, Okta, Google, SCIM] }` | End. Caller must use supported provider. Custom providers require platform engineering review. | Platform only integrates with providers for which it has tested security and sync logic; new providers require engineering review. |
| `SR_GOV_02_BE-02` | BE | governance | `webhook_endpoint` is not HTTPS or resolves to a private IP range. | URL validator + DNS resolver | Validation layer | Registration with http:// URL or RFC1918 / link-local IP | Request rejected with 400; audit event `iam_webhook_invalid_endpoint` | Same | `ValidationError { code: 400, reason: "webhook_endpoint must be public HTTPS" }` | End. Caller uses public HTTPS endpoint. | Webhook endpoints must be publicly reachable over TLS so the IAM provider can push events. Private IPs indicate misconfiguration and HTTP endpoints violate the transport security baseline. |
| `SR_GOV_03` | --- | governance | Receive an IAM user-sync webhook event, verify the signature against the stored signing key, map the event (create/update/delete) to platform user state, update Neo4j `Person` node and PostgreSQL `iam_sync_state`, and emit a governance event. | `REUSABLE_CaaSCredentialRetriever` (for signing key), `REUSABLE_TenantFilter`, `REUSABLE_EventBusPublisher`, `REUSABLE_AuditLogger` | Inbound webhook POST from IAM provider | `IamWebhookEvent { provider_ref, event_type, user_attributes, signature }` | `Person` node upserted in Neo4j, `iam_sync_state.last_event_at` updated, event published to bus, audit row written | `IamWebhookEvent` (JSON, provider-specific normalized) | `WebhookAck { accepted: bool, event_id }` | `SR_GOV_05` (role mapping) on user-create or user-role-change. `SR_GOV_13` on user-delete. `End` on attribute-only update. | Near-real-time sync keeps platform identity in step with the source of truth (the customer's IAM) with latency <1 minute typical. |
| `SR_GOV_03_SE-01` | SE | governance | Signature verification fails (tampering, expired key, or wrong key). | CaaS signing key, signature library | Verification library returns false | Same IamWebhookEvent | Event rejected with 401; audit event `iam_webhook_signature_failed` written; security officer alerted if rate exceeds 5/minute | Same | `WebhookAck { accepted: false, reason: "signature_invalid" }` | End (no retry). Repeated failures escalate to security review. | Signature verification is the only trust boundary for webhook events. Accepting unverified events would allow identity injection. |
| `SR_GOV_03_SE-02` | SE | governance | Neo4j write fails during `Person` node upsert (transient failure). | Durable retry queue | Driver exception | Same IamWebhookEvent | Event queued for retry; `WebhookAck` returned 202 Accepted with retry semantics | Same | `WebhookAck { accepted: true, deferred: true, retry_after }` | Retry up to 5 times over 5 minutes; on exhaustion, alert admin and mark sync as DEGRADED. | Sync must not lose events; retry with idempotent upsert keyed on `iam_object_id` guarantees eventual consistency. |
| `SR_GOV_03_BE-01` | BE | governance | Webhook event references an `iam_object_id` not yet known to the platform, but the event type is not "create" (e.g., "update" or "delete" for unknown user). | Lookup against `Person` node by `iam_object_id` | Neo4j MATCH returns no Person for update/delete event | Same IamWebhookEvent | Event queued for deferred processing pending user creation; if pending > 1 hour, alert admin and optionally drop (admin-configurable) | Same | `WebhookAck { accepted: true, deferred: true, reason: "pending_user_creation" }` | Deferred queue. Poll for user creation every 30s; on arrival, apply. | Out-of-order events are common with bulk IAM changes; deferred processing prevents spurious errors and preserves sync integrity. |
| `SR_GOV_03_BE-02` | BE | governance | Webhook event's tenant reference does not match any registered provider (possible misrouting or replay). | `REUSABLE_TenantFilter` + provider lookup | Provider_ref unknown | Same IamWebhookEvent | Event rejected with 404; audit event `iam_webhook_unknown_provider` | Same | `WebhookAck { accepted: false, reason: "unknown_provider" }` | End. Security officer notified if rate exceeds 1/minute. | Protects against accidental cross-tenant event leakage and replay attacks. |
| `SR_GOV_04` | --- | governance | On a scheduled cadence (default 15 minutes), poll each registered IAM provider for changes since `iam_sync_state.last_event_at` as a fallback for missed webhook events. | IAM provider SDKs, `REUSABLE_CaaSCredentialRetriever` | Scheduled job per tenant per provider | `PollRequest { tenant_id, provider_ref, since_timestamp }` | List of user-change events applied identically to `SR_GOV_03`; `iam_sync_state.last_poll_at` updated; delta count emitted for monitoring | `PollRequest` (JSON) | `PollResult { events_applied, events_failed, next_poll_at }` | For each change, `SR_GOV_03` logic applies. | Webhook delivery is best-effort from the provider side; a polling fallback guarantees eventual consistency if webhook events are dropped or the platform is temporarily unreachable. |
| `SR_GOV_04_SE-01` | SE | governance | IAM provider API returns 429 (rate limited) during polling. | Back-off scheduler | Provider response | Same PollRequest | Poll deferred; next attempt at `Retry-After` header value or default 5 minutes | Same | `PollResult { status: "rate_limited", deferred_until }` | Defer and retry. If rate limit persists for 4 consecutive polls, alert admin. | Respecting provider rate limits avoids provider-side blocks that would break sync entirely. |
| `SR_GOV_04_SE-02` | SE | governance | IAM provider API returns 5xx (provider outage). | Provider health tracker | Provider response | Same PollRequest | Poll marked as failed; `iam_sync_state.last_error` set; alert if failure > 1 hour | Same | `PollResult { status: "provider_error", retry_in }` | Retry at next scheduled poll. After 4 consecutive failures, degrade tenant's sync status and notify admin. | Provider outages are external and must be handled gracefully without degrading platform availability. |
| `SR_GOV_05` | --- | governance | Map IAM groups/roles from a user sync event to platform roles according to the tenant's role-mapping table; create or update `ROLE_OF` edges in Neo4j. | `REUSABLE_TenantFilter`, role_mapping table in PostgreSQL | Triggered from `SR_GOV_03` or `SR_GOV_04` when user's role changes | `RoleMappingInput { tenant_id, person_iam_ref, iam_groups[] }` | `ROLE_OF` edges upserted for each mapped platform role; `Person.roles[]` updated; audit event `user_roles_mapped` written | `RoleMappingInput` (JSON) | `RoleMappingResult { person_id, platform_roles[], unmapped_iam_groups[] }` | If `unmapped_iam_groups` non-empty: `SR_GOV_06` (conflict detection). Else: `End`. | Deterministic role mapping translates the customer's IAM group semantics into the platform's role model, enabling consistent authorization across every layer. |
| `SR_GOV_05_BE-01` | BE | governance | The role-mapping table for this tenant is empty or has never been configured. | Tenant role_mapping table | Lookup returns zero rows for tenant_id | Same RoleMappingInput | Platform assigns default `unmapped_user` role (read-only); admin notified to configure mapping; user flagged in UI as `pending_role_mapping` | Same | `RoleMappingResult { status: "default_assigned", action_required: "configure_role_mapping" }` | End (user remains read-only until admin configures mapping). | Default-deny posture: unmapped users get minimal access instead of escalated access, preventing accidental over-permissioning. |
| `SR_GOV_05_BE-02` | BE | governance | An IAM group maps to multiple conflicting platform roles (ambiguous mapping). | Role mapping rules | Multiple matches in mapping table | Same RoleMappingInput | User flagged as `role_conflict`, no platform roles granted, admin alerted for manual resolution | Same | `RoleMappingResult { status: "conflict", conflicting_roles[] }` | `SR_GOV_06` | Preventing silent resolution enforces D-21: contradictions are flagged and resolved by an administrator, not silently auto-resolved. |
| `SR_GOV_06` | --- | governance | Detect and resolve conflicts between platform governance and IAM source of truth per D-21: platform governance wins for platform-specific resources, IAM wins for identity, unresolvable contradictions block the affected user until admin resolution. | `REUSABLE_AuditLogger`, `REUSABLE_Alerter` | Triggered from `SR_GOV_05` on unmapped groups or conflicting mappings, or from drift detection jobs comparing IAM state to platform state | `ConflictReport { tenant_id, person_id?, resource_id?, iam_value, platform_value, conflict_type }` | Resolution outcome: either auto-applied (identity direction → IAM wins), blocked (affected user suspended, admin notified), or deferred to admin decision queue with structured context | `ConflictReport` (JSON) | `ConflictResolution { outcome, blocked_until?, admin_action_required }` | If blocked: `End` with user suspended. If auto-resolved: `End`. If deferred: remains in admin queue until `SR_GOV_06` re-triggered by admin decision. | Enforces D-21 and prevents silent resolution, which is explicitly forbidden. Silent resolution would create either over-permissioning (security risk) or under-permissioning (productivity loss) with no audit trail. |
| `SR_GOV_06_BE-01` | BE | governance | Admin has not resolved a conflict within the configured SLA (default 72 hours). | SLA tracker | Timer expiration | `ConflictReport` still in `deferred` status | User remains suspended; escalation alert to tenant admin manager + platform security officer; SLA breach recorded | Same | `SlaBreach { conflict_id, escalated_to }` | Alert escalates until resolved. User cannot access platform during this time. | SLA breach visibility ensures that unresolved conflicts cannot silently persist and that accountability is preserved. |
| `SR_GOV_07` | --- | governance | Platform-managed tenant (no external IAM) invites a user by email; an invitation token is generated and stored, and the invitation email is queued. | Platform signing key (CaaS), email provider integration, `REUSABLE_AuditLogger` | Called from `SR_GOV_01` when IAM provider is `standalone`, or later by tenant admin | `InvitationRequest { tenant_id, email, role_ids[], expiry_hours }` | `user_invitations` row inserted with hashed token, `Person` node (stub) created in Neo4j, invitation email sent, audit event `user_invited` | `InvitationRequest` (JSON) | `InvitationResult { invitation_id, expires_at }` | `SR_GOV_08` when user clicks invitation link | Standalone mode supports the SMB segment (5-20 employees) that lacks external IAM; without it, ~30% of the target market cannot be served. |
| `SR_GOV_07_SE-01` | SE | governance | Email provider returns error (SMTP failure, invalid recipient format handled at provider). | Durable email queue | Provider error | Same InvitationRequest | Email queued for retry up to 5 times over 30 minutes; on exhaustion, mark invitation as `send_failed` and notify admin | Same | `InvitationResult { status: "send_failed", remediation }` | Admin can re-send manually; system will not auto-resend after exhaustion. | Email is best-effort; retry with bounded attempts plus admin visibility prevents silent invitation failures. |
| `SR_GOV_07_BE-01` | BE | governance | Caller is not the tenant admin or lacks the `user_invite` permission. | Role check | Authorization middleware | InvitationRequest + caller principal | Rejected 403; audit event `invite_forbidden` | Same | `AuthError { code: 403 }` | End. Escalates to security review if repeated. | Invitation is a privileged action that can change tenant permission surface. Unauthorized attempts are logged and rate-limited. |
| `SR_GOV_07_BE-02` | BE | governance | Email address already belongs to an active user in this tenant. | Email uniqueness check | Duplicate check at request time | Same | Rejected 409; audit event `invite_duplicate_email` | Same | `ValidationError { code: 409 }` | End. Admin should use user management panel to adjust existing user's roles instead. | Prevents the "two accounts one email" failure mode that leads to access confusion. |
| `SR_GOV_08` | --- | governance | A user completes platform-managed authentication by clicking the invitation link, setting a password + enrolling MFA, or requesting a magic link; session is issued as a JWT in an httpOnly/Secure/SameSite cookie per D-68. | `REUSABLE_CaaSCredentialRetriever`, platform JWT signing key, MFA provider (TOTP/email/SMS), `REUSABLE_AuditLogger` | User-initiated auth (browser) | `AuthAttempt { invitation_token? OR email+password OR magic_link_token, mfa_code? }` | Session established; JWT issued (1 hour) with refresh token (7 days, rotation on use); `Person` node status set to `active`; audit event `user_authenticated` | `AuthAttempt` (JSON) | `AuthResult { access_token_cookie_set, refresh_token_cookie_set, session_id }` | End (user is authenticated). Subsequent calls pass through token validation middleware. | Provides identity proofing for standalone-mode tenants with MFA as the authentication baseline. httpOnly cookies prevent XSS token theft per D-68. |
| `SR_GOV_08_SE-01` | SE | governance | MFA provider (TOTP/SMS/email) is unreachable during login. | MFA provider, fallback path | Provider timeout | Same AuthAttempt | User shown temporary failure message; login retry allowed with exponential backoff; if repeated, offer admin-assisted MFA reset | Same | `AuthError { code: 503, retry_after }` | Retry. Alert SRE if >5% of MFA attempts fail in any 5-minute window. | MFA is mandatory for admins and configurable for others; provider outages must be handled without leaking which factor failed or allowing bypass. |
| `SR_GOV_08_BE-01` | BE | governance | Invitation token has expired or is already consumed. | Invitation tracking table | Lookup | `AuthAttempt { invitation_token }` | Login refused; audit event `invite_token_invalid`; user shown option to request new invitation from admin | Same | `AuthError { code: 410, reason: "invitation_expired_or_used" }` | End. Admin must re-invite. | Expiring tokens prevent long-lived invitation tokens from being exploited if leaked. |
| `SR_GOV_08_BE-02` | BE | governance | Password does not meet complexity policy (length, character classes, breach-list check). | Password policy evaluator, breach-list service | Policy check | Same AuthAttempt with password | Set-password rejected; user given specific remediation; audit event `password_policy_violation` | Same | `ValidationError { code: 422, policy_requirements }` | User retries with stronger password. | Weak passwords are the single most common initial access vector; enforcement at set time is more effective than post-hoc detection. |
| `SR_GOV_08_BE-03` | BE | governance | Failed login attempts exceed threshold (default 5 within 15 minutes). | Rate limiter + CaaS account state | Counter check | Same AuthAttempt | Account temporarily locked (15 minutes); audit event `account_lockout`; user notified via email | Same | `AuthError { code: 423, unlock_at }` | User waits or contacts admin for manual unlock. | Blunts online password guessing without enabling denial-of-service against arbitrary users (lockout is temporary). |
| `SR_GOV_09` | --- | governance | Migrate a standalone-mode tenant to an IAM-synced model by matching existing platform users to IAM users by email, converting standalone auth records to IAM identities, and disabling standalone password auth. | `REUSABLE_CaaSCredentialRetriever`, IAM provider SDKs, Neo4j, PostgreSQL | Tenant admin triggers migration from admin panel | `MigrationRequest { tenant_id, target_provider, match_strategy: email } ` | Users matched and rebound to `iam_object_id`; platform_credentials entries archived; migration audit events written; standalone auth disabled after successful match of all admin users | `MigrationRequest` (JSON) | `MigrationResult { matched_count, unmatched_users[], rollback_available_until }` | End, pending 7-day rollback window. | Allows SMBs that outgrow standalone mode to adopt Entra/Okta without data loss — a key retention lever. |
| `SR_GOV_09_BE-01` | BE | governance | One or more admin users cannot be matched to an IAM user (email mismatch or admin missing from IAM directory). | Admin match check | Match lookup | Same MigrationRequest | Migration HALTED before standalone auth is disabled; admin shown unmatched list and given option to manually map or cancel | Same | `MigrationResult { status: "halted", unmatched_admins[] }` | Admin resolves and re-runs `SR_GOV_09`. | Preserves at least one working admin path at all times (D-37 minimum-admin rule). Without this, the migration could lock everyone out. |

---

## Section 2 — Employee Lifecycle (SR_GOV_10 through SR_GOV_15)

Covers: D-35 employee lifecycle events — new hire, promotion / role change with transition window, lateral move, departure / immediate revocation, temporary elevation, inactivity timeout.

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_10` | --- | governance | A new hire is provisioned: `Person` node created from IAM event or admin invitation, default role mapping applied, default compartment membership assigned per the tenant's vertical defaults, audit event written, welcome notification queued. | `REUSABLE_TenantFilter`, `REUSABLE_AuditLogger`, role_mapping table | Triggered from `SR_GOV_03` (IAM webhook user-create) or `SR_GOV_07` (standalone invitation acceptance) | `NewHireEvent { tenant_id, person_iam_ref, email, display_name, department, manager_iam_ref?, hire_date }` | `Person` node created in Neo4j; default `ROLE_OF` edges per `SR_GOV_05`; default `COMPARTMENT_MEMBER` edges; `Person.status = active`; audit event `person_provisioned` | `NewHireEvent` (JSON) | `ProvisioningResult { person_id, default_roles, default_compartments }` | `SR_GOV_05` (role mapping) | Provides a single deterministic provisioning path that produces identical results regardless of which trigger fires (IAM sync vs standalone invitation acceptance). |
| `SR_GOV_10_BE-01` | BE | governance | Manager IAM ref is provided but does not exist in the platform yet (out-of-order events). | Person lookup by `iam_object_id` | Match returns nothing | Same NewHireEvent | Person provisioned without manager edge; deferred binding queued for retry every 5 minutes for 1 hour | Same | `ProvisioningResult { person_id, manager_binding: deferred }` | Deferred queue. After 1 hour, alert admin to map manager manually. | Manager-employee chain is required for DRPRR (FOUND § 1.4.2) approval routing; missing chains create silent escalation failures. |
| `SR_GOV_10_BE-02` | BE | governance | Department name in event does not match any known department in the tenant's organizational tree. | Department registry | Lookup against `Department` nodes | Same | Person provisioned with department flagged as `unmapped`; admin alerted; person retains default tenant-wide compartment until department is resolved | Same | `ProvisioningResult { person_id, department_status: "unmapped" }` | Admin uses department mapping panel to resolve. | Department mapping drives many governance rules (compartment membership, approval routing, query analytics scope); leaving it ambiguous would create silent rule misapplication. |
| `SR_GOV_11` | --- | governance | An employee's role changes (promotion or lateral move with level change). The new role is granted, the old role is revoked after a configurable transition window (default 7 days, range 0-30 days), and audit events are written for both grant and scheduled revoke. | `REUSABLE_AuditLogger`, scheduled task system, `REUSABLE_TenantFilter` | IAM webhook user-update event with role delta, OR admin-initiated role change | `RoleChangeEvent { tenant_id, person_id, old_role_ids[], new_role_ids[], transition_window_days?, reason }` | New `ROLE_OF` edges created immediately; existing `ROLE_OF` edges marked `revoke_at = now() + window`; scheduled task created to perform revoke; audit events `role_granted` and `role_revoke_scheduled` written | `RoleChangeEvent` (JSON) | `RoleChangeResult { granted_roles, revoke_at, transition_window_days }` | Scheduled `SR_GOV_11_REVOKE` at `revoke_at`. | Transition window prevents silent loss of access during a role change while still ending old access in a bounded time. Default 7 days is configurable per tenant. |
| `SR_GOV_11_REVOKE` | --- | governance | Scheduled task fires at `revoke_at`, removes the old `ROLE_OF` edges, terminates active sessions associated with revoked role-only permissions, and writes the corresponding audit event. | Scheduled task runner, session manager, `REUSABLE_AuditLogger` | Scheduled task firing | `RoleRevokeJob { tenant_id, person_id, role_ids[] }` | Old `ROLE_OF` edges removed; affected sessions terminated; audit event `role_revoked` written | `RoleRevokeJob` (JSON) | `RoleRevokeResult { revoked_count, sessions_terminated }` | End | Ensures the transition window has a deterministic terminal action. Without explicit revoke, residual access could persist indefinitely. |
| `SR_GOV_11_BE-01` | BE | governance | The role change would leave the tenant with zero active admin users. | Admin count check | Pre-change validation | Same RoleChangeEvent | Change rejected with 409; audit event `admin_lockout_prevented`; admin instructed to grant another user admin role first | Same | `ValidationError { code: 409, reason: "would_leave_no_admins" }` | End. Admin must add another admin first. | Hard rule: no role change may leave a tenant without at least one active admin (D-37 minimum-admin rule). |
| `SR_GOV_12` | --- | governance | Lateral move within the same role level: scope or department changes but role identity stays. The `Person.department` is updated, compartment membership is recalculated, and any compartment-bound subscriptions are reassigned. | `REUSABLE_TenantFilter`, `REUSABLE_AuditLogger`, compartment recalculation worker | IAM event with department-only delta, OR admin update | `LateralMoveEvent { tenant_id, person_id, old_department, new_department }` | `Person.department` updated; compartment membership recomputed; audit event `lateral_move` written | `LateralMoveEvent` (JSON) | `LateralMoveResult { person_id, compartments_added[], compartments_removed[] }` | End | Distinguishes a lateral move from a promotion to keep role-based grants stable while adjusting scope-based grants. |
| `SR_GOV_13` | --- | governance | An employee departs (IAM disable, manual termination, or contractor end-date): all `ROLE_OF` edges removed immediately, all active sessions terminated, all delegated user credentials revoked from CaaS, owned `ServiceAccount` nodes flagged for review, and audit retained per retention policy. | `REUSABLE_CaaSCredentialRetriever` (revoke), session manager, `REUSABLE_AuditLogger`, `REUSABLE_Alerter` | IAM webhook user-delete OR user-disable event, OR admin termination action | `TerminationEvent { tenant_id, person_id, reason, termination_at }` | All `ROLE_OF` removed; sessions terminated; delegated credentials revoked from CaaS; owned ServiceAccounts flagged; `Person.status = terminated`; audit event `person_terminated`; admin notified of orphaned service accounts | `TerminationEvent` (JSON) | `TerminationResult { person_id, sessions_terminated, credentials_revoked, orphaned_service_accounts[] }` | End | Departure must propagate immediately to all access surfaces; delayed revocation is the most common cause of insider threat realization. |
| `SR_GOV_13_SE-01` | SE | governance | CaaS is unreachable when attempting to revoke delegated credentials. | Durable retry queue, `REUSABLE_Alerter` | CaaS exception | Same TerminationEvent | Person.status set to `terminated`; sessions terminated; CaaS revocation queued for retry; security officer alerted with severity HIGH | Same | `TerminationResult { credentials_revoke: deferred }` | Retry with exponential backoff for 1 hour; on exhaustion, escalate to manual revocation. | Delaying termination because CaaS is down would leave attack window open; prefer to terminate sessions and audit immediately, retry credential revocation. |
| `SR_GOV_13_BE-01` | BE | governance | Termination would leave the tenant without an active admin. | Admin count check | Pre-termination validation | Same TerminationEvent | Termination DEFERRED; admin alerted; manual override available with explicit acknowledgment | Same | `ValidationError { code: 409, deferred_until_resolution }` | Admin assigns another user to admin role, then re-fires `SR_GOV_13`. | Same protective rule as `SR_GOV_11_BE-01`: never zero admins. |
| `SR_GOV_14` | --- | governance | Temporary elevation grants a person elevated permissions (e.g., admin-level access for an operations task) for a bounded period via dual approval per GAP-49; access automatically revokes at expiration. | `REUSABLE_ApprovalChainResolver`, scheduled task runner, `REUSABLE_AuditLogger` | Person submits `ElevationRequest`; two approvers from `REUSABLE_ApprovalChainResolver` must approve | `ElevationRequest { tenant_id, person_id, requested_permissions[], duration_minutes, justification }` | Pending approval state; on dual approval, temporary `ROLE_OF` edge with `expires_at` created; on expiration, automatic revocation; audit events at request, approve, grant, and revoke | `ElevationRequest` (JSON) | `ElevationResult { state, expires_at?, approval_chain[] }` | On approval: scheduled `SR_GOV_14_REVOKE`. | Provides an auditable, time-boxed alternative to standing privileged access. Without it, operators would either lack access or hold permanent elevated access (the most common cause of privilege creep). |
| `SR_GOV_14_REVOKE` | --- | governance | At elevation expiration, revoke the temporary role, terminate sessions reliant on it, and write the corresponding audit event. | Scheduled task runner, session manager, `REUSABLE_AuditLogger` | Scheduler firing | `ElevationRevokeJob { tenant_id, person_id, role_id }` | Temporary `ROLE_OF` removed; sessions terminated; audit event `elevation_revoked` written | `ElevationRevokeJob` (JSON) | `ElevationRevokeResult { revoked }` | End | Guarantees elevation cannot persist past its time-box. |
| `SR_GOV_14_BE-01` | BE | governance | Requested elevation duration exceeds the per-tenant maximum (default 24 hours). | Tenant policy lookup | Validation | Same ElevationRequest | Request rejected; user instructed to request shorter duration or use break-glass for true emergencies | Same | `ValidationError { code: 422, max_duration_minutes }` | End. Caller resubmits or uses `SR_GOV_46` (break-glass). | Bounding maximum elevation duration prevents elevation from being used as a back door for permanent access. |
| `SR_GOV_14_BE-02` | BE | governance | Approval chain has fewer than 2 eligible approvers (e.g., a small tenant with only one admin). | Approval chain resolver | Resolver returns insufficient approvers | Same ElevationRequest | Request rejected; admin instructed to add a second approver or use break-glass for emergency | Same | `ValidationError { code: 412, eligible_approvers }` | End. Caller adds approver or uses break-glass. | Dual approval is structurally required by GAP-49; bypass via break-glass is the only auditable alternative. |
| `SR_GOV_15` | --- | governance | Periodic job scans `Person` records for inactivity exceeding the tenant policy (default 90 days for standalone, IAM-driven for IAM-synced); inactive users are auto-disabled with admin notification. | Scheduled scanner, `REUSABLE_AuditLogger`, `REUSABLE_Alerter` | Daily scheduled job per tenant | `InactivityScanRequest { tenant_id, threshold_days }` | List of inactive `Person` IDs; each set to `disabled` (NOT terminated); audit event `inactivity_disable` per person; admin notification with re-enable workflow | `InactivityScanRequest` (JSON) | `InactivityScanResult { disabled_count, persons_disabled[] }` | End | Inactive accounts accumulate risk; auto-disable shrinks the attack surface without permanently destroying data tied to the user. |
| `SR_GOV_15_BE-01` | BE | governance | Disabling would leave the tenant with zero active admins. | Admin count check | Pre-disable validation | InactivityScanRequest | Skip disabling the last admin; alert tenant owner to designate another admin or re-activate this one; audit event `inactivity_disable_skipped` | Same | `InactivityScanResult { skipped_admin_ids[] }` | End. Manual remediation by tenant owner. | Same protective rule: never zero admins. |

---

## Section 3 — Governance Enforcement Model (SR_GOV_16 through SR_GOV_22)

Covers: D-20 ENFORCE vs ADVISE rule evaluation, override justification, rule versioning, conflict detection, rollback, and audit export.

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_16` | --- | governance | Evaluate an ENFORCE rule against a candidate action and return ALLOW or DENY. ENFORCE rules cover PII/PHI/CUI classification, visibility compartment boundaries, multi-tenant isolation, security-classified data handling, and admin-set security/compliance rules; they cannot be overridden. | `governance_rules` table, `REUSABLE_TenantFilter`, `REUSABLE_ComplianceProfileLookup`, `REUSABLE_AuditLogger` | Inline call from any layer about to perform a governance-relevant action | `RuleEvaluationRequest { tenant_id, action, subject_principal, object_resource, attributes, rule_classes: [ENFORCE] }` | `RuleEvaluationResult { decision: ALLOW \| DENY, matched_rules[], reason }`; on DENY, audit event `enforce_denied`; the calling layer must abort the action | `RuleEvaluationRequest` (JSON) | `RuleEvaluationResult` (JSON) | If ALLOW: caller proceeds. If DENY: caller terminates action (cannot override). | ENFORCE rules implement the non-overridable security and compliance baseline (D-20). Allowing override would void compliance posture. |
| `SR_GOV_16_SE-01` | SE | governance | The governance rule cache or `governance_rules` table is unreachable. | Local rule cache + DB fallback | Cache miss + DB exception | Same `RuleEvaluationRequest` | DENY by default; audit event `enforce_failsafe_deny` written; SRE alerted | Same | `RuleEvaluationResult { decision: DENY, reason: "rule_engine_unavailable" }` | Caller aborts. Retry only after rule engine is healthy. | Failsafe-deny is the only safe default when rule evaluation cannot occur — ALLOW would be an open door. |
| `SR_GOV_16_SE-02` | SE | governance | Rule evaluation exceeds the SLA latency budget (default 50 ms; configurable). | Latency timer | Timer expiration | Same | DENY with reason `evaluation_timeout`; audit event written; SRE alerted on rate >1% | Same | `RuleEvaluationResult { decision: DENY, reason: "timeout" }` | Caller aborts. SRE investigates rule complexity or DB performance. | Slow governance evaluation creates DoS vectors and user-experience degradation; the failsafe-deny posture preserves security while making the failure visible. |
| `SR_GOV_17` | --- | governance | Evaluate an ADVISE rule against a candidate action; return ALLOW (default), or ALLOW_WITH_WARNING, or REQUIRE_JUSTIFICATION; the caller may override REQUIRE_JUSTIFICATION by providing a justification per `SR_GOV_18`. | `governance_rules` (advise class), `REUSABLE_AuditLogger` | Inline call from any layer about to perform a governance-relevant action that may be advised against | `RuleEvaluationRequest { tenant_id, action, subject, object, attributes, rule_classes: [ADVISE] }` | `RuleEvaluationResult { decision, advisory_messages[], requires_justification: bool }`; advisory recorded in audit | `RuleEvaluationRequest` (JSON) | `RuleEvaluationResult` (JSON) | If `requires_justification`: `SR_GOV_18`. Otherwise caller proceeds. | ADVISE rules let experts override operational defaults (e.g., data freshness, confidence thresholds) with accountability via justification capture. |
| `SR_GOV_18` | --- | governance | Capture an ADVISE override justification, validate that it is non-empty, relevant (rejects "because" / "ok" / "n/a" / "i don't know"), and links it to the action and the rule that was overridden. | Justification validator (length, content), `REUSABLE_AuditLogger` | User submits override after `SR_GOV_17` returned `requires_justification` | `OverrideJustification { tenant_id, person_id, action_id, rule_id, justification_text, category? }` | Justification stored linked to action; audit event `advise_override_justified` written; action proceeds | `OverrideJustification` (JSON) | `OverrideJustificationResult { accepted: bool, action_id }` | If accepted: caller proceeds. Else: rejected, caller must improve justification. | Aligns with D-19: rejections and overrides without meaningful justification add no value to the learning loop. Validation enforces minimum quality. |
| `SR_GOV_18_BE-01` | BE | governance | Justification text fails the relevance check (too short, blocklist match, or fails category-specific validation). | Validator | Pattern match + length | Same OverrideJustification | Rejected; user shown specific guidance on what is missing | Same | `ValidationError { code: 422, hints }` | User refines and resubmits. | Without quality enforcement, the override field becomes pure noise and erodes the learning loop. |
| `SR_GOV_19` | --- | governance | Publish a new version of a governance rule by creating a `RulesetVersion` row, validating the rule grammar, dry-running against a recent decision sample, and atomically promoting if dry-run passes. | Rule grammar parser, recent decision sample, atomic version promotion, `REUSABLE_AuditLogger` | Tenant security officer or platform admin via admin panel | `RulePublishRequest { tenant_id, rule_set, change_description, dry_run_sample_size? }` | New `RulesetVersion`; dry-run report; on success, version promoted atomically; audit event `rule_published` | `RulePublishRequest` (JSON) | `RulePublishResult { version_id, dry_run_report, promoted: bool }` | End. | Dry-running new rules against historical decisions catches over-broad or under-specified rules before production rollout, preventing governance regressions. |
| `SR_GOV_19_BE-01` | BE | governance | Dry-run shows the new rule would have changed >5% of recent decisions in unexpected ways. | Decision delta analysis | Threshold check | Same RulePublishRequest | Promotion BLOCKED; admin shown delta report; explicit confirmation required to override the threshold | Same | `RulePublishResult { promoted: false, delta_report }` | Admin reviews delta. Override available with explicit confirmation per BP-86. | Bounded delta protects against accidental mass denials or mass authorizations from a single rule edit. |
| `SR_GOV_20` | --- | governance | Detect conflicts between governance rules within a single ruleset (e.g., one rule says ALLOW for X, another says DENY for X). | Conflict analyzer (rule set walker) | On `SR_GOV_19` publish OR scheduled nightly scan | `ConflictScanRequest { tenant_id, ruleset_version }` | List of conflicting rule pairs with conflict type; published as `RuleConflictReport` to admin | `ConflictScanRequest` (JSON) | `RuleConflictReport { conflicts[], severity }` | If severity HIGH: blocks `SR_GOV_19` promotion. If LOW: warning emitted but promotion allowed. | Rule conflicts are silent failure modes; explicit detection prevents inconsistent governance enforcement. |
| `SR_GOV_21` | --- | governance | Roll back to a prior `RulesetVersion` atomically, with audit and an automated dry-run on the rollback target to confirm it still applies cleanly. | Atomic ruleset switch, decision sample, `REUSABLE_AuditLogger` | Admin-initiated rollback OR automated rollback on production rule failure | `RuleRollbackRequest { tenant_id, target_version_id, reason }` | Rollback executed; audit event `rule_rolled_back` written; admin notified | `RuleRollbackRequest` (JSON) | `RuleRollbackResult { active_version, rollback_reason }` | End | Provides a fast escape hatch when a published ruleset causes unexpected failures or user impact. Bounded recovery time. |
| `SR_GOV_22` | --- | governance | Export the audit-ready record of all rules in effect for a tenant on a given date for regulatory or examiner review. | Versioned ruleset store, audit trail, signing key | Compliance officer, regulatory examiner (REPW path) | `RuleExportRequest { tenant_id, as_of_date, format: pdf \| json \| csv }` | Signed export bundle including ruleset, change history, and audit context | `RuleExportRequest` (JSON) | `RuleExportResult { export_url, signature, expires_at }` | End | Regulators must be able to see exactly which rules were in force at any historical point — a core auditability requirement (GAP-71, GAP-80). |

---

## Section 4 — Combination Sensitivity Assessment / CSA (SR_GOV_23 through SR_GOV_30)

Covers: D-34 CSA — the mosaic-effect prevention engine that runs before Decision Support combines data from multiple `DataCollection` nodes. CSA rules are configurable per tenant, evaluated by a deterministic engine, and result in one of three actions: BLOCK, ANONYMIZE, ELEVATE.

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_23` | --- | governance | A tenant security officer registers a new CSA rule (e.g., "PII + financial = HIGH"); the rule is parsed, validated against the rule grammar, dry-run against recent Decision Support combines, and atomically activated. | `csa_rules` table, rule grammar parser, recent combine sample, `REUSABLE_AuditLogger` | Admin panel | `CsaRuleRegistration { tenant_id, rule_expression, action: BLOCK \| ANONYMIZE \| ELEVATE, severity, dry_run_sample_size? }` | `csa_rules` row inserted (versioned); dry-run report; on success activated; audit event `csa_rule_registered` | `CsaRuleRegistration` (JSON) | `CsaRuleResult { rule_id, version, dry_run_report, active }` | End. The rule is now evaluated by `SR_GOV_25` on every CSA assessment. | CSA rules must be configurable per tenant because the dangerous combinations differ by vertical (financial vs healthcare vs government). Centralizing rule registration with dry-run prevents over-broad rules from breaking decision support. |
| `SR_GOV_23_BE-01` | BE | governance | Rule expression contains a syntax error or references unknown attributes. | Grammar parser | Parser error | Same `CsaRuleRegistration` | Rejected; admin shown precise parse error and acceptable attributes | Same | `ValidationError { code: 422, parse_error, hints }` | Admin corrects and resubmits. | Syntax errors must surface at registration time, not at evaluation time. Run-time syntax errors would convert into failsafe denials. |
| `SR_GOV_24` | --- | governance | Trigger a CSA assessment when Decision Support is about to combine data from N >= 2 `DataCollection` nodes. The assessment uses the active CSA rules for the tenant and the combined attribute set (PII / PHI / CUI / financial / location / temporal / group_size / external). | `REUSABLE_TenantFilter`, active csa_rules, `REUSABLE_AuditLogger` | Inline call from Decision Support before query execution | `CsaAssessmentRequest { tenant_id, query_id, data_collection_refs[], combined_attribute_set, query_purpose }` | `CsaAssessment` node persisted in Neo4j (BP-43); decision returned; audit event `csa_assessed` | `CsaAssessmentRequest` (JSON) | `CsaAssessmentResult { decision, applied_rules[], required_action }` | If decision is ALLOW: `End` (caller proceeds). If BLOCK / ANONYMIZE / ELEVATE: caller routes to `SR_GOV_26 / 27 / 28`. | The mosaic-effect risk is real: individually low-sensitivity datasets become high-sensitivity when combined. CSA must run BEFORE the combine to prevent the combined result from existing in memory. |
| `SR_GOV_25` | --- | governance | Evaluate the active CSA rules against a `CsaAssessmentRequest`'s combined attribute set, returning the highest-severity action. | csa_rules cache, rule evaluator | Inline call from `SR_GOV_24` | `CsaEvaluationInput { tenant_id, attribute_set, group_size, query_purpose }` | Rule evaluation outcome including matched rules and the resulting action | `CsaEvaluationInput` (JSON) | `CsaEvaluationOutput { matched_rules[], highest_action }` | Returned to `SR_GOV_24`. | Pure-function evaluator separated from the side-effecting trigger and from the action handlers; testable in isolation. |
| `SR_GOV_26` | --- | governance | CSA action BLOCK: refuse to combine the data; return an explainable rejection to the caller; offer alternatives (e.g., "request elevated permission", "request anonymized aggregate"). | `REUSABLE_AuditLogger` | Called from `SR_GOV_24` when action = BLOCK | `CsaBlockAction { csa_assessment_id, reason, suggested_alternatives[] }` | Decision Support call rejected; user shown rejection with alternatives; audit event `csa_blocked` written | `CsaBlockAction` (JSON) | `CsaBlockResult { rejected: true, alternatives }` | End | Explainable rejection preserves user trust ("I see why this was blocked") while preventing the dangerous combination. |
| `SR_GOV_27` | --- | governance | CSA action ANONYMIZE: invoke the Re-identification Risk Assessment Pipeline (RRAP, GAP-62) and the platform's anonymization function to produce an aggregated/anonymized version of the result before returning to caller. | RRAP component, anonymization function, `REUSABLE_AuditLogger` | Called from `SR_GOV_24` when action = ANONYMIZE | `CsaAnonymizeAction { csa_assessment_id, data_collection_refs[], target_k_anonymity, aggregation_strategy }` | Anonymized result set with documented anonymization parameters; audit event `csa_anonymized` | `CsaAnonymizeAction` (JSON) | `CsaAnonymizeResult { anonymized_payload, parameters_applied, residual_risk_score }` | Caller proceeds with anonymized result. | Allows the analytical value to be delivered while neutralizing the re-identification risk via documented privacy parameters. |
| `SR_GOV_28` | --- | governance | CSA action ELEVATE: require the caller to obtain elevated permissions (typically a higher role or compartment membership) before allowing the combine; route through `SR_GOV_14` if appropriate. | `REUSABLE_ApprovalChainResolver`, `REUSABLE_AuditLogger` | Called from `SR_GOV_24` when action = ELEVATE | `CsaElevateAction { csa_assessment_id, required_permission, justification_required }` | Caller informed of required permission; if user has the permission they may retry; if not they may request elevation via `SR_GOV_14` | `CsaElevateAction` (JSON) | `CsaElevateResult { required_permission, request_path }` | If user has permission: caller retries `SR_GOV_24` with elevated context. Else: `SR_GOV_14`. | ELEVATE allows authorized roles to perform sensitive combinations under explicit accountability rather than blanket-blocking the analytical value. |
| `SR_GOV_29` | --- | governance | Bypass CSA via break-glass: an emergency override path requiring two-person activation per GAP-49, time-boxed (**4-hour default** per BP-133), fully audited, with retroactive review required within **48 hours** (BP-133). | `REUSABLE_ApprovalChainResolver` (break-glass approvers), `REUSABLE_AuditLogger`, post-incident review queue | User invokes break-glass on a CSA-blocked operation | `CsaBreakGlassRequest { csa_assessment_id, justification, approver_1, approver_2, duration_minutes (default 240) }` | Bypass granted only after both approvers confirm out-of-band (different channels); time-boxed at 4 hours default; combine allowed once; review queued for post-incident review within 48 hours | `CsaBreakGlassRequest` (JSON) | `CsaBreakGlassResult { authorized, expires_at, review_id }` | Caller proceeds within time-box. Review at `SR_GOV_29_REVIEW`. | Break-glass provides an auditable escape hatch for genuine emergencies without weakening the default-deny posture. 4-hour default is reasonable for emergency access; 48-hour review window accommodates weekends (per BP-133 workbook reconciliation). |
| `SR_GOV_29_REVIEW` | --- | governance | Within **48 hours** (per BP-133) of any CSA break-glass use, the security officer reviews the use, classifies it as JUSTIFIED / UNJUSTIFIED / NEEDS_RULE_REFINEMENT, and writes the review outcome to audit. SLA breach on review escalates via `SR_GOV_45`. | `REUSABLE_AuditLogger`, SLA timer | Scheduled review task | `BreakGlassReviewInput { review_id, review_decision, notes }` | Review row written; if UNJUSTIFIED triggers security review with the user; if NEEDS_RULE_REFINEMENT triggers a CSA rule review | `BreakGlassReviewInput` (JSON) | `BreakGlassReviewResult { review_decision, follow_ups[] }` | End | Forces the second half of the break-glass loop: every emergency use is reviewed and either justified or escalated. 48-hour window accommodates weekends without allowing indefinite normalization (BP-133). |
| `SR_GOV_30` | --- | governance | Persist every CSA assessment as a `CsaAssessment` node (BP-43) and link it to the originating query and the data collections involved, providing a permanent audit record. | `REUSABLE_AuditLogger`, Neo4j | Called inline from `SR_GOV_24` | `CsaAssessmentPersistInput { csa_assessment_id, query_id, data_collection_refs[], result }` | `CsaAssessment` node created with `ASSESSED_BY` edge to query and `INVOLVED` edges to collections; audit row written | `CsaAssessmentPersistInput` (JSON) | `CsaAssessmentPersistResult { node_id }` | End | Persistent audit of every assessment supports both compliance review and learning loops to refine CSA rules over time. |

---

## Section 5 — Visibility Compartments (SR_GOV_31 through SR_GOV_36)

Covers: visibility compartment creation, membership assignment, access checks, revocation, criminal-penalty data isolation override, and compartment audit (per trunk COMP § 3.1.1, GAP-77).

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_31` | --- | governance | Tenant security officer creates a Visibility Compartment with classification level, allowed roles, and explicit member roster. | `compartments` table, `REUSABLE_AuditLogger` | Admin panel | `CompartmentCreateRequest { tenant_id, name, classification_level, member_roles[], member_persons[], purpose, criminal_penalty_isolation: bool }` | `Compartment` node + row in `compartments`; audit event `compartment_created` written | `CompartmentCreateRequest` (JSON) | `CompartmentCreateResult { compartment_id }` | End | Compartments are the primary mechanism for criminal-penalty data isolation (BSA/AML, healthcare substance abuse, government classification). They override the default "visibility flows up" model. |
| `SR_GOV_31_BE-01` | BE | governance | Compartment requests `criminal_penalty_isolation = true` but the tenant is not on a vertical that supports it. | Vertical compatibility check | Compliance profile lookup | Same | Rejected; user instructed which verticals support criminal-penalty isolation | Same | `ValidationError { code: 422 }` | End | Vertical-specific features must not be silently downgraded; the user must understand the constraint. |
| `SR_GOV_32` | --- | governance | Add a person or role to a compartment after the requesting admin's authorization is verified. | `REUSABLE_AuditLogger`, role check | Admin panel | `CompartmentMembershipAdd { tenant_id, compartment_id, person_id?, role_id? }` | `COMPARTMENT_MEMBER` edge created in Neo4j; audit event `compartment_member_added` | `CompartmentMembershipAdd` (JSON) | `CompartmentMembershipResult { added }` | End. Triggers re-evaluation of the affected user's effective compartment set. | Provides explicit, audited compartment access. Without explicit membership, criminal-penalty data is invisible (default-deny). |
| `SR_GOV_33` | --- | governance | Check whether a principal can access a resource bound to a compartment; deny if principal is not a member of every compartment the resource belongs to. | Compartment edges, `REUSABLE_TenantFilter` | Inline call from any data layer about to expose data | `CompartmentAccessCheck { tenant_id, principal, resource_id, resource_compartments[] }` | ALLOW or DENY decision | `CompartmentAccessCheck` (JSON) | `CompartmentAccessResult { decision, reason }` | If DENY: caller terminates exposure. If ALLOW: caller proceeds. | Compartment checks gate every data exposure. They are the only mechanism that enforces criminal-penalty isolation. |
| `SR_GOV_34` | --- | governance | Revoke compartment membership for a person or role; emit audit event; terminate any active sessions exposing data from that compartment. | `REUSABLE_AuditLogger`, session manager | Admin panel or person termination | `CompartmentMembershipRemove { tenant_id, compartment_id, person_id?, role_id? }` | `COMPARTMENT_MEMBER` edge removed; sessions touching compartment-bound data terminated; audit event `compartment_member_removed` | `CompartmentMembershipRemove` (JSON) | `CompartmentMembershipResult { removed }` | End | Membership revocation must propagate immediately; lingering session-level access defeats the compartment. |
| `SR_GOV_35` | --- | governance | Override the default "visibility flows up" rule for criminal-penalty data: even an executive cannot see compartment data unless explicitly added; CSA and other inferential checks must respect compartment isolation. | Compartment evaluator | Inline check from `SR_GOV_33` | `CriminalPenaltyOverrideCheck { compartment_id, principal_chain }` | DENY for any principal not explicitly listed, regardless of position in org tree | `CriminalPenaltyOverrideCheck` (JSON) | `OverrideResult { decision: DENY \| ALLOW }` | Returned to `SR_GOV_33`. | Overrides the default LCA "ancestors see descendants' data" rule for compartments where statutory penalties exist for unauthorized disclosure (e.g., 31 USC § 5318(g)(2) for BSA/AML tipping-off). |
| `SR_GOV_36` | --- | governance | Generate a compartment audit report showing membership history, access events, and any anomalies for a given period. | `REUSABLE_AuditLogger`, query layer | Admin panel or scheduled report | `CompartmentAuditRequest { tenant_id, compartment_id, period }` | Signed report PDF/CSV with full membership and access timeline | `CompartmentAuditRequest` (JSON) | `CompartmentAuditResult { report_url, signature }` | End | Quarterly compartment review is required by most regulated verticals; the audit report enables both internal and external review. |

---

## Section 6 — Query Analytics Governance (SR_GOV_37 through SR_GOV_40)

Covers: D-17 query analytics — capture, aggregation, access control, export. Three privacy levels: anonymous, role-based, individual.

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_37` | --- | governance | Capture a `QueryAnalyticsEvent` for every query passed through the platform; the privacy level (anonymous / role / individual) is determined by the tenant's policy and the user's consent. | `query_analytics` table, `REUSABLE_TenantFilter` | Inline call from LLM Router and Decision Support after a query completes | `QueryAnalyticsEvent { tenant_id, query_id, query_type_hash, complexity_tier, model_used, response_time_ms, outcome, privacy_level }` | Row inserted into `query_analytics`; per privacy-level, individual fields are stripped or aggregated | `QueryAnalyticsEvent` (JSON) | `AnalyticsCaptureResult { recorded }` | End | Provides the data substrate for D-17 organizational intelligence (Flywheel 2) without sacrificing user privacy. |
| `SR_GOV_38` | --- | governance | Aggregate query analytics into role-, department-, and tenant-level summaries on a periodic schedule (default hourly). | Aggregation worker, `query_analytics` | Scheduled job | `AggregationRequest { tenant_id, period }` | Aggregated rows in `query_analytics_aggregates`; cached for dashboard | `AggregationRequest` (JSON) | `AggregationResult { rows_processed, aggregates_written }` | End | Enables fast-load dashboards by precomputing rollups; avoids repeating expensive queries on every dashboard load. |
| `SR_GOV_39` | --- | governance | Enforce access control on query analytics: anonymous data is visible to anyone; role-based data is visible to department heads and C-suite; individual data is visible only to the user themselves and a designated admin with audit. | Access control matrix, `REUSABLE_AuditLogger` | Inline call from interface or admin API | `AnalyticsAccessRequest { tenant_id, principal, requested_scope, requested_subject? }` | ALLOW / DENY decision; on ALLOW, the analytics row(s) are returned | `AnalyticsAccessRequest` (JSON) | `AnalyticsAccessResult { decision, rows? }` | End | Aligns with D-17 privacy levels and prevents the platform's organizational intelligence feature from becoming a surveillance feature. |
| `SR_GOV_40` | --- | governance | Export query analytics for offline analysis or compliance review; export inherits the access control from `SR_GOV_39` and is signed with the tenant key. | Export builder, signing key | Admin panel | `AnalyticsExportRequest { tenant_id, period, scope, format }` | Signed export bundle | `AnalyticsExportRequest` (JSON) | `AnalyticsExportResult { export_url, signature }` | End | Customers and auditors must be able to extract analytics data; signing provides tamper evidence. |

---

## Section 7 — Approval Chains, LCA, Delegation, and Break-Glass (SR_GOV_41 through SR_GOV_46)

Covers: trunk LCA approval algorithm (FOUND § 1.4.1), DEF delegation (FOUND § 1.4.6), break-glass (GAP-49). These SRs invoke the trunk algorithms; they do not re-derive them.

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_41` | --- | governance | Create an approval request for any governance-relevant action: store the request, compute the approval chain via `REUSABLE_ApprovalChainResolver`, notify approvers, set an SLA timer per `REUSABLE_DelegationFramework`. | `REUSABLE_ApprovalChainResolver`, `REUSABLE_AuditLogger`, `REUSABLE_Alerter` | Inline call from any layer requesting an approval-gated action | `ApprovalRequest { tenant_id, action, requested_by, payload, sla_tier? }` | `ApprovalRequest` row created with state `PENDING`; approver list assigned; notifications dispatched; SLA timer set | `ApprovalRequest` (JSON) | `ApprovalRequestResult { approval_id, approvers[], sla_deadline }` | `SR_GOV_43` (chain execution) | Centralized request creation ensures every approval flows through LCA (FOUND § 1.4.1) and is audit-trail-visible. |
| `SR_GOV_42` | --- | governance | Compute the LCA approval chain for a given action and set of participating principals; resolve dual-reporting paths via DRPRR (FOUND § 1.4.2). | Org tree (Neo4j), DRPRR rules engine | Called from `SR_GOV_41` | `LcaComputeInput { tenant_id, action, principals[] }` | Ordered approval chain from leaf to LCA | `LcaComputeInput` (JSON) | `LcaComputeResult { chain[] }` | Returned to `SR_GOV_41`. | LCA + DRPRR are the deterministic algorithms that prevent ambiguity in dual-reporting hierarchies; centralizing them ensures consistent application. |
| `SR_GOV_43` | --- | governance | Execute the approval chain: present each approver in turn, capture decisions, advance on APPROVE, terminate on REJECT, escalate on SLA breach via `SR_GOV_45`. | `REUSABLE_AuditLogger`, `REUSABLE_Alerter`, scheduled task system | Triggered from `SR_GOV_41` | `ApprovalChainExecution { approval_id, current_index }` | State machine progresses: PENDING → APPROVED / REJECTED / DEFERRED / EXPIRED | `ApprovalChainExecution` (JSON) | `ApprovalChainResult { final_state, decisions[] }` | If APPROVED: callback to the requesting layer to perform the action. If REJECTED: callback to inform the requester. If EXPIRED: `SR_GOV_45`. | Step-by-step execution with explicit state transitions ensures every approval is auditable and recoverable from any state. |
| `SR_GOV_44` | --- | governance | Delegate approval authority from approver A to approver B for a bounded period via DEF (FOUND § 1.4.6); the delegation is recorded and propagates to all in-flight approvals where A is the next approver. | DEF rules, `REUSABLE_AuditLogger` | Approver A submits delegation | `DelegationRequest { tenant_id, from_person, to_person, scope, expires_at }` | Delegation row created; in-flight approvals re-routed; audit event `delegation_created` | `DelegationRequest` (JSON) | `DelegationResult { delegation_id, affected_approvals[] }` | End. Delegation expires automatically. | DEF allows continuity during planned absences without permanent privilege transfer. |
| `SR_GOV_45` | --- | governance | On SLA breach, escalate the approval to the next level above the current approver per DEF tier (5d/2d/24h/4h), or to the configured escalation contact. | DEF tiered timers, `REUSABLE_Alerter`, scheduled task system | SLA timer expiration | `EscalationRequest { approval_id, current_approver, new_approver }` | Approval re-assigned to new approver; original approver notified; audit event `approval_escalated`; new SLA timer started | `EscalationRequest` (JSON) | `EscalationResult { reassigned_to, new_deadline }` | Returns to `SR_GOV_43`. | Without escalation, requests would stall on busy approvers; tiered SLAs (DEF) ensure urgent requests find an approver within hours, not days. |
| `SR_GOV_46` | --- | governance | Initiate a break-glass approval bypassing the normal chain: requires two-person activation via different channels, time-boxed grant (**4-hour default** per BP-133), full audit, mandatory post-incident review within **48 hours** (BP-133). | `REUSABLE_AuditLogger`, `REUSABLE_Alerter`, post-incident review queue | Emergency invocation | `BreakGlassRequest { tenant_id, action, requested_by, justification, second_approver, duration_minutes (default 240) }` | Two-person check; on success, action authorized for the duration (4 hours default); review row queued | `BreakGlassRequest` (JSON) | `BreakGlassResult { authorized, expires_at, review_id }` | After expiration, action authority lapses. Review at `SR_GOV_46_REVIEW`. | Provides the only authorized override for emergencies. Two-person + retroactive review prevents the override from becoming a normal access path. 4-hour default per workbook BR_GOV_08; 48-hour review accommodates weekends. |
| `SR_GOV_46_REVIEW` | --- | governance | Within **48 hours** of break-glass use, the security officer reviews the use, classifies it, and writes the review to audit. SLA breach escalates via `SR_GOV_45`. | `REUSABLE_AuditLogger`, SLA timer | Scheduled review task | `BreakGlassReviewInput { review_id, review_decision, notes }` | Review row written; if UNJUSTIFIED a security investigation is triggered | `BreakGlassReviewInput` (JSON) | `BreakGlassReviewResult` | End | Closes the loop on every emergency override. 48-hour window per BP-133. |

---

## Section 8 — Audit Trail (SR_GOV_47 through SR_GOV_52)

Covers: D-22 event-sourced audit with Merkle hash chain, GAP-71 audit trail, COMP § 3.3.3 crypto-shredding.

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_47` | --- | governance | Append an audit event to the `audit_events` table via `SR_DM_05` (which handles partition routing per `SR_DM_06`); compute the next Merkle hash using `prev_event_hash` + serialized event content; persist row; emit to event bus for live consumers. Replayed events from offline mode (`SR_UI_36`) preserve their original timestamps as `payload.original_timestamp` metadata while the row's `created_at` reflects actual write time. | `audit_events` table via `SR_DM_05`, `REUSABLE_MerkleChainHasher`, `REUSABLE_EventBusPublisher` | Inline call from any governance, runtime, or interface action | `AuditEvent { tenant_id, event_type, principal, target, payload, source_layer, severity, prev_event_hash, original_timestamp? }` | Row inserted via `SR_DM_05` with `event_hash`, `chain_position`, and `prev_event_hash`; published to bus | `AuditEvent` (JSON) | `AuditCaptureResult { event_id, chain_position }` | End | Append-only with hash chain provides tamper evidence without blockchain overhead. Original-timestamp preservation supports BP-112 offline replay semantics. |
| `SR_GOV_47_SE-01` | SE | governance | PostgreSQL is unavailable when writing the audit row. | Local durable spool, `REUSABLE_Alerter` | DB exception | Same `AuditEvent` | Event spooled to local durable storage; alert SRE; retried on DB recovery | Same | `AuditCaptureResult { state: "spooled", spool_id }` | Replay from spool when DB recovers. | Audit must never be lost; spooling preserves the event with eventual consistency. |
| `SR_GOV_48` | --- | governance | Verify the current chain head by recomputing the last N hashes against persisted values; raise alarm on mismatch. | `audit_events` table, `REUSABLE_MerkleChainHasher` | Scheduled hourly job + on-demand for compliance reviews | `ChainVerificationRequest { tenant_id, depth_n }` | Verification result (PASS/FAIL); on FAIL the security officer is alerted with severity CRITICAL | `ChainVerificationRequest` (JSON) | `ChainVerificationResult { result, mismatch_at?, anchor_hash }` | If FAIL: incident response. If PASS: end. | Periodic verification catches storage-layer tampering or corruption that bypassed application-level controls. |
| `SR_GOV_49` | --- | governance | Query the audit trail with a tenant filter, action filter, principal filter, time range, and severity filter; results are paginated and signed. | `audit_events`, `REUSABLE_TenantFilter` | Admin panel or compliance API | `AuditQueryRequest { tenant_id, filters, time_range, page_size, page_token? }` | Page of audit events; each event includes `event_hash` for verification | `AuditQueryRequest` (JSON) | `AuditQueryResult { events[], next_page_token, signature }` | Caller may iterate. | Provides the basic forensic and compliance read path; signed results enable independent verification. |
| `SR_GOV_50` | --- | governance | Export an audit slice for regulatory or examiner review; format is signed, includes hash anchor, and the chain segment proving integrity. | Export builder, signing key | Compliance officer or REPW (GAP-80) | `AuditExportRequest { tenant_id, time_range, format }` | Signed export bundle with chain proof | `AuditExportRequest` (JSON) | `AuditExportResult { export_url, signature, chain_proof }` | End | Regulators must be able to verify the export against the live chain anchor; chain proof prevents redaction or fabrication. |
| `SR_GOV_51` | --- | governance | When `SR_GOV_48` reports tamper or chain breakage, freeze the affected tenant's writes, alert the platform security officer with severity CRITICAL, and initiate the incident response workflow. | Tenant write-freeze switch, `REUSABLE_Alerter`, incident response runbook | Triggered by `SR_GOV_48` | `TamperResponseInput { tenant_id, mismatch_at, anchor_hash }` | Tenant writes paused; admin notified; incident ticket created | `TamperResponseInput` (JSON) | `TamperResponseResult { freeze_active, incident_id }` | Manual recovery process. | Tamper detection without response is useless; freezing writes preserves the rest of the chain while investigation proceeds. |
| `SR_GOV_52` | --- | governance | Crypto-shred a subject's data per COMP § 3.3.3: identify all encrypted events scoped to the subject's per-subject AES-256 key, invoke `SR_DM_30` to destroy the key in CaaS following NIST SP 800-88 verified destruction and mark the encrypted events as cryptographically inaccessible, write a destruction certificate. Also invoked from tenant offboarding (`SR_INT_30`). | CaaS key destroy operation via `SR_DM_30`, `REUSABLE_AuditLogger`, NIST 800-88 verifier | Subject erasure request from rights officer OR tenant offboarding (`SR_INT_30`) | `CryptoShredRequest { tenant_id, subject_id, requested_by, justification, source: rights_request \| tenant_offboarding }` | Per-subject key destroyed via `SR_DM_30`; encrypted events become unreadable; destruction certificate written; audit event `crypto_shredded` | `CryptoShredRequest` (JSON) | `CryptoShredResult { destruction_certificate, verified_at }` | End | Provides a defensible erasure mechanism that satisfies privacy regulators (EDPB 02/2025) without rewriting the immutable event log. The explicit `SR_DM_30` invocation ensures the data-model-level marking step always occurs (BP-96, BP-102). |

---

## Section 9 — Classification and Data Handling Governance (SR_GOV_53 through SR_GOV_60)

Covers: data classification assignment, override, downgrade approval (BP-30), retention policy (BP-28), bulk import approval (BP-26), schema change response (BP-27), cross-tenant learning opt-in (BP-31), training data consent (BP-56).

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_53` | --- | governance | Assign a security classification (PII / PHI / CUI / financial / public / internal) to a `DataField` based on Stage 1-2 classification results from the Connection Layer. The classification defaults to MORE restrictive on tie-break and enters a low-confidence queue if confidence <0.7. | Three-tier NER ensemble (GAP-66), `data_classifications` registry, `REUSABLE_AuditLogger` | Inline call from Connection Layer Stage 2 | `ClassificationInput { tenant_id, datafield_id, candidate_classifications[], confidence_scores[] }` | DataField tagged with classification; if confidence below threshold, queued for human review per BP-09 | `ClassificationInput` (JSON) | `ClassificationResult { applied, requires_review, queue_id? }` | If confidence high: end. If low: human review queue. | Classification decisions cascade across every later access decision; default-restrictive on uncertainty preserves compliance posture. |
| `SR_GOV_54` | --- | governance | A reviewer corrects a classification (raise or lower); the change is persisted in `classification_overrides` and applied to future pulls of the same field via the override map (BP-09). Downgrades require approval per `SR_GOV_55`. | `classification_overrides`, `REUSABLE_AuditLogger` | Reviewer panel | `ClassificationCorrection { tenant_id, datafield_id, new_classification, reason }` | Override row inserted; future pulls apply override before tagging; audit event `classification_overridden` | `ClassificationCorrection` (JSON) | `ClassificationCorrectionResult { override_id }` | If downgrade: route through `SR_GOV_55` first. Else: end. | Persistent overrides prevent the loop where the same correction must be made repeatedly on every new pull. |
| `SR_GOV_55` | --- | governance | A classification downgrade (e.g., PII → internal) requires the security officer's explicit approval and a stronger justification per BP-30. | `REUSABLE_ApprovalChainResolver`, `REUSABLE_AuditLogger` | Triggered from `SR_GOV_54` for downgrade | `DowngradeApprovalRequest { tenant_id, datafield_id, current, target, justification }` | Approval chain (single approver: security officer); on approval, override applied; audit event `classification_downgraded` | `DowngradeApprovalRequest` (JSON) | `DowngradeApprovalResult { approved, justification }` | If approved: returns to `SR_GOV_54`. Else: end with rejection. | Downgrades increase exposure surface; making them explicit and approval-gated prevents silent compliance regressions. |
| `SR_GOV_56` | --- | governance | Apply a tenant data retention policy: scan `DataCollection` and `DataField` nodes against the policy, archive or delete past-retention data including any items currently in the connection-layer quarantine queue (`SR_CONN_32`), write the audit event. Retention policies are admin-approved per BP-28 and not retroactive by default. | `data_retention_policies`, archival store, `REUSABLE_AuditLogger`, `REUSABLE_QuarantineQueue` reader | Daily scheduled job per tenant | `RetentionApplyRequest { tenant_id, include_quarantine: bool }` | Past-retention data archived or deleted per the policy action including matching quarantine items; audit event `retention_applied` | `RetentionApplyRequest` (JSON) | `RetentionApplyResult { archived_count, deleted_count, quarantine_purged_count }` | End | Retention compliance is a regulatory requirement (HIPAA, GLBA, state privacy laws); explicit application makes it auditable. Quarantine inclusion ensures no orphaned data escapes retention enforcement (BP-125). |
| `SR_GOV_57` | --- | governance | Approve a bulk import (Type 6 per Spec 03) before it begins ingesting data: classification scan on a sample, approval by tenant admin, throttle settings configured. | `REUSABLE_ApprovalChainResolver`, sample classifier, `REUSABLE_AuditLogger` | Admin panel or API initiated bulk import | `BulkImportApprovalRequest { tenant_id, source_description, expected_record_count, sample, requested_by }` | Sample classified; admin approves; row in `bulk_import_log`; throttle profile assigned | `BulkImportApprovalRequest` (JSON) | `BulkImportApprovalResult { approval_id, throttle_profile }` | Connection Layer proceeds with import per `SR_CONN_` rows. | Bulk imports can dwarf normal usage; pre-approval and throttling prevent operational and compliance accidents. |
| `SR_GOV_58` | --- | governance | Respond to a detected schema change in an external system per BP-27 by classifying severity (minor / moderate / major) and applying the matching response: minor (auto-update edges), moderate (alert + pause non-essential pulls), major (alert + pause all pulls + require approval). | Severity classifier, `REUSABLE_Alerter` | Triggered by Intelligence Layer schema change subscription | `SchemaChangeResponseInput { tenant_id, system_id, change_type, change_details }` | Severity assigned; appropriate response applied; audit event `schema_change_response` | `SchemaChangeResponseInput` (JSON) | `SchemaChangeResponseResult { severity, response_action }` | End. Major severity schedules `SR_GOV_41` approval. | Severity-based response prevents minor schema drifts from blocking the platform while ensuring major drifts get human review before resuming. |
| `SR_GOV_59` | --- | governance | Manage the per-tenant opt-in for cross-tenant aggregate learning (BP-31). Default is OPT-OUT; opt-in requires explicit tenant admin action and is recorded in audit. The opt-in state is read by `SR_INT_22` aggregate workers before any aggregation runs; toggling OFF takes effect within one aggregation cycle (max 24 hours). | Tenant settings table, `REUSABLE_AuditLogger` | Admin panel | `CrossTenantLearningToggle { tenant_id, opt_in: bool, scope[], approved_by }` | Tenant settings updated; learning workers (`SR_INT_22`) respect the new value within one cycle; audit event `cross_tenant_learning_changed` | `CrossTenantLearningToggle` (JSON) | `LearningToggleResult { active, takes_effect_at }` | End | Default-deny on cross-tenant learning protects customer data; explicit opt-in with admin signature provides defensible consent. Bidirectional reference with `SR_INT_22` ensures the toggle is honored at both the policy and operational layers (BP-126). |
| `SR_GOV_60` | --- | governance | Manage per-collection training-data consent (BP-56) for fine-tuning per D-42 and GAP-44 model collapse prevention. Default is OPT-OUT; opt-in records consent with justification. The consent state is read by `SR_LLM_50` (fine-tune lifecycle) and `SR_LLM_51` (per-batch consent check) before any training run. | `data_collection.training_consent` field, `REUSABLE_AuditLogger` | Admin panel or per-collection API | `TrainingConsentToggle { tenant_id, collection_id, opt_in: bool, approved_by }` | Field updated; downstream training pipeline (`SR_LLM_50`, `SR_LLM_51`) respects the value; audit event `training_consent_changed` | `TrainingConsentToggle` (JSON) | `ConsentToggleResult { active }` | End | Required by GAP-44 (60% human-data minimum) and customer trust; default-deny prevents accidental inclusion in training data. Bidirectional reference with `SR_LLM_50` makes the consent gate explicit at both ends (BP-127). |

---

## Section 10 — Component, Service Account, and Model Governance (SR_GOV_61 through SR_GOV_66)

Covers: BP-85 component lifecycle approval, BP-86 AI-generated code approval, BP-92 service account quarterly review, BP-57 prompt template approval, D-29 model hot-swap approval.

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_61` | --- | governance | Approve a component for activation per its category's approval chain (Spec 08). Categories have different approvers: Universal Connectors → platform admin; Auth Handlers → security officer; Discovery/Tagging Agents → platform admin + security officer; Generated Components → admin + human review. Invoked from `SR_CAT_15`. The approval is granted only after `SR_V2_01` field-validation precondition passes. | `REUSABLE_ApprovalChainResolver`, component_registry, `REUSABLE_AuditLogger` | Inline call from `SR_CAT_15` (component lifecycle transition testing → active) | `ComponentApprovalRequest { tenant_id, component_id, category, version, change_summary }` | Approval chain executed (after `SR_V2_01` precondition); on approval, component status set to `active`; audit event `component_approved` | `ComponentApprovalRequest` (JSON) | `ComponentApprovalResult { approved, approvers[] }` | If approved: component activates via `SR_CAT_15`. Else: returned to component owner. | Approval-gated activation enforces the principle that no untested or unreviewed code runs in production. The bidirectional reference to `SR_CAT_15` and the V2 precondition link complete BP-113 and BP-123. |
| `SR_GOV_62` | --- | governance | AI-generated component code requires an additional security scan (SAST + dependency check + supply-chain check) plus mandatory human review per BP-86; failures BLOCK approval. Invoked from `SR_CAT_22` when component `created_by = ai_generated`. | Security scanner, dependency analyzer, `REUSABLE_AuditLogger` | Inline call from `SR_CAT_22` AI generation workflow | `AiCodeReviewInput { component_id, code_artifact, security_scan_report, human_reviewer? }` | Either passes (proceeds to `SR_GOV_61` decision) or fails (blocked, owner notified) | `AiCodeReviewInput` (JSON) | `AiCodeReviewResult { passed, findings, requires_remediation }` | If passed: returns to `SR_CAT_22` and then `SR_GOV_61`. Else: blocked. | AI-generated code is more likely to introduce subtle vulnerabilities (hallucinated APIs, insecure defaults); the additional gate prevents these from reaching production. Bidirectional reference to `SR_CAT_22` completes BP-113. |
| `SR_GOV_63` | --- | governance | Quarterly review of every active service account: scope still correct?, anomalous usage?, still needed?; reviewer marks for retirement, scope change, or continuation. Invoked by `SR_SA_30`. | `service_account_registry`, anomaly metrics, `REUSABLE_AuditLogger` | Inline call from `SR_SA_30` (scheduled quarterly job per tenant) | `SaQuarterlyReviewRequest { tenant_id }` | Review tasks created and routed to appropriate reviewers; outcomes recorded; audit events written | `SaQuarterlyReviewRequest` (JSON) | `SaQuarterlyReviewResult { reviewed_count, retired_count, scope_changed_count }` | End | Service accounts are the most common attack target; quarterly review surfaces orphaned and over-permissioned accounts before they are exploited. Bidirectional reference to `SR_SA_30` completes BP-116. |
| `SR_GOV_64` | --- | governance | Respond to an SA anomaly (per `SR_SA_` exception rows) per BP-92: classify anomaly type, apply auto-suspension if criteria met, notify admin, queue for review. Invoked by `SR_SA_45` auto-suspension and the per-anomaly detector SRs `SR_SA_36` through `SR_SA_43`. | SA anomaly registry, suspend operation, `REUSABLE_Alerter` | Inline call from `SR_SA_45` or any `SR_SA_36`-`SR_SA_43` detector | `SaAnomalyEvent { tenant_id, sa_id, anomaly_type, severity, evidence }` | Anomaly logged; if criteria met: SA suspended via `SR_SA_14` pending review; admin alerted via `SR_GOV_67` | `SaAnomalyEvent` (JSON) | `SaAnomalyResponseResult { action_taken, review_queued }` | End. Manual reactivation by admin after review. | Auto-suspension shrinks blast radius for compromised SAs; mandatory review prevents auto-suspension from becoming a denial-of-service vector. Bidirectional references to `SR_SA_45` and `SR_SA_14` complete BP-117. |
| `SR_GOV_65` | --- | governance | Approve a tenant prompt template customization per BP-57: validate that the customization does not override safety guardrails, run a sandbox test, then activate. | Prompt validator, sandbox tester, `REUSABLE_AuditLogger` | Admin panel | `PromptCustomizationApprovalRequest { tenant_id, template_id, content, requested_by }` | Validation result; if passes, activated; audit event `prompt_template_approved` | `PromptCustomizationApprovalRequest` (JSON) | `PromptCustomizationResult { approved, validation_findings }` | End | Tenant prompt customizations could otherwise weaken safety guardrails; mandatory validation prevents this without blocking legitimate customization. |
| `SR_GOV_66` | --- | governance | Approve a model hot-swap per D-29: only platform admin + security admin may invoke; tenant whitelist/blacklist enforced; canary deployment required (5% traffic, 7 days); auto-rollback if quality drops >10%. | `REUSABLE_ApprovalChainResolver`, model_registry, canary controller, `REUSABLE_AuditLogger` | Model registry update | `ModelHotSwapRequest { tenant_id_or_global, slot, candidate_model, canary_config, justification }` | Approval chain executed; canary deployment initiated; rollback policy active | `ModelHotSwapRequest` (JSON) | `ModelHotSwapResult { approval_id, canary_id }` | Canary monitor at LLM Routing layer determines promotion or rollback. | Model swaps materially affect every output; gating + canary + auto-rollback contains blast radius. |

---

## Section 11 — Operational Governance (SR_GOV_67 through SR_GOV_72)

Covers: BP-29 alert routing, BP-76 feature flags, BP-77 admin undo, connection consent, coverage disclosure enforcement, rejection justification validation per D-19.

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_67` | --- | governance | Route an alert via the severity matrix per BP-29: CRITICAL → page on-call + SMS + in-app + email; HIGH → in-app + email; MEDIUM → in-app + digest; LOW → digest only. Each tenant may customize the matrix within bounds. | `alert_history`, alert routing matrix, `REUSABLE_Alerter` | Inline call from any layer raising an alert | `AlertEvent { tenant_id, severity, source, message, attribution }` | Alert dispatched per matrix; row in `alert_history`; acknowledgement tracking enabled | `AlertEvent` (JSON) | `AlertDispatchResult { recipients[], dispatch_ids[] }` | End | Alert fatigue is the failure mode that breaks every alerting system; the severity matrix prevents over-alerting on low-severity events. |
| `SR_GOV_68` | --- | governance | Toggle a per-tenant feature flag per BP-76; flags are platform-admin approved and may be plan-tier gated. Toggle takes effect within 60 seconds across stateless services via cache invalidation. | `feature_flags` table, cache invalidator, `REUSABLE_AuditLogger` | Admin panel | `FeatureFlagToggleRequest { tenant_id, flag_id, value, approved_by }` | Flag updated; cache invalidated; audit event `feature_flag_toggled` | `FeatureFlagToggleRequest` (JSON) | `FeatureFlagResult { active }` | End | Feature flags enable progressive rollout, kill switches, and per-tenant feature gating without code deploys. |
| `SR_GOV_69` | --- | governance | Provide undo for critical admin actions within a configurable window (default 10 minutes) per BP-77; security-critical actions are NOT undoable. | Action history, `REUSABLE_AuditLogger` | Admin clicks "undo" within window | `UndoRequest { tenant_id, action_id, requesting_admin }` | If action is undoable and within window: original action reverted; audit event `admin_undone` | `UndoRequest` (JSON) | `UndoResult { undone, reason_if_not? }` | End | Undo recovers from human error without weakening security (security-critical actions remain irreversible). |
| `SR_GOV_70` | --- | governance | Capture explicit tenant consent for a new external system connection (Spec 03 Type 1-8); consent records who authorized, when, scope, vendor terms acknowledged, and (for paywalled APIs per `SR_CONN_42` / D-54) the explicit `paywall_acknowledgement` field with vendor ToS reference. | `connections` table, `REUSABLE_AuditLogger` | New connection wizard or `SR_CONN_42` paywalled flow | `ConnectionConsentRequest { tenant_id, system_id, connection_type, scope, vendor_terms_acknowledged, paywall_acknowledgement?: { vendor_tos_url, accepted_at, accepted_by }, authorized_by }` | Consent row inserted with paywall acknowledgement when applicable; audit event `connection_consented`; connection moves from `REQUESTED` to `APPROVED` (`SR_CONN_` lifecycle) | `ConnectionConsentRequest` (JSON) | `ConnectionConsentResult { consent_id, paywall_recorded: bool }` | Connection Layer proceeds. | Explicit consent prevents accidental access expansion and provides legal basis for vendor terms compliance (D-54 paywalled API governance). The optional `paywall_acknowledgement` payload satisfies BP-98 and creates a defensible record of vendor ToS acceptance for audit. |
| `SR_GOV_71` | --- | governance | Enforce that every Decision Support response includes a coverage disclosure showing what % of relevant data the platform has visibility into; reject responses missing the disclosure. | Coverage calculator (Spec 04), response validator | Inline call from Decision Support before delivery | `CoverageEnforcementInput { tenant_id, response_payload, query_context }` | Response either passes (coverage disclosure present and accurate) or is rejected for re-generation | `CoverageEnforcementInput` (JSON) | `CoverageEnforcementResult { passed, missing_fields? }` | If passed: end. Else: response regenerated. | Per D-14 transparency requirement: every recommendation must disclose coverage. Without enforcement at the governance layer, coverage disclosures could drift or be omitted. |
| `SR_GOV_72` | --- | governance | Validate a user's recommendation rejection justification per D-19: must be non-empty, must select a valid category, must include free-text content of **at least 20 characters** (per BP-134), must pass the filler-word blocklist (no / nope / because / idk / n/a), must pass T1 relevance scoring against the recommendation topic with score ≥0.5; invalid rejections are not stored. | Rejection validator with 20-char minimum, filler-word blocklist, T1 relevance scorer, `REUSABLE_AuditLogger` | User submits rejection from interface | `RejectionInput { tenant_id, recommendation_id, category, justification_text }` | Validated rejection stored; learning loop notified; audit event `rejection_recorded`; invalid rejections returned to interface with specific remediation | `RejectionInput` (JSON) | `RejectionResult { stored, validation_findings? }` | End | Without validation, rejections become a noisy signal that degrades the learning loop. D-19 enforces structured, meaningful feedback. 20-char minimum and relevance scoring per BP-134 close the obvious loopholes. |

---

## Section 12 — Governance Integration Points (SR_GOV_73 through SR_GOV_78)

Covers: governance hooks invoked from each non-governance layer. These SRs define the external contracts that other layers must call before performing governance-relevant actions.

| ID | Type | Layer | Usecase | Assets / Cred | Input Source / Condition | Expected Input | Expected Output | Input Format | Output Format | Next Step | Why |
|----|------|-------|---------|---------------|-------------------------|----------------|-----------------|--------------|--------------|----------|-----|
| `SR_GOV_73` | --- | governance | LLM Router Stage 1 invocation: deterministic governance evaluation of an LLM request to produce the allowed model set per D-10. Combines `SR_GOV_16` (ENFORCE) results with the 8 stage-1 rules from Spec 05. Invoked by `SR_LLM_05`. | Stage 1 rule registry, `REUSABLE_TenantFilter` | Inline call from `SR_LLM_05` on every model request | `RouterStage1Input { tenant_id, principal, data_attributes, request_context }` | Allowed-model set or DENY; audit event `router_stage1_evaluated` | `RouterStage1Input` (JSON) | `RouterStage1Result { allowed_models[], reasoning[] }` | If allowed set non-empty: returns to `SR_LLM_05` for Stage 2. Else: DENY. | Centralizes the deterministic governance gate so every model invocation passes through identical rules. Bidirectional reference to `SR_LLM_05` completes BP-104. |
| `SR_GOV_74` | --- | governance | Decision Support inline check: invoke `SR_GOV_24` (CSA), `SR_GOV_71` (coverage), and `SR_GOV_18` (override justification when needed) before executing a recommendation generation. Invoked by `SR_DS_05`. | CSA, coverage calculator, justification validator | Inline call from `SR_DS_05` before generation | `DecisionSupportPreflightInput { tenant_id, query_id, data_collection_refs[], parameter_overrides[] }` | Aggregated preflight result; on any DENY, generation aborted with explainable reason | `DecisionSupportPreflightInput` (JSON) | `DecisionSupportPreflightResult { allowed, blocked_reasons[] }` | If allowed: `SR_DS_05` proceeds. Else: end with explanation. | Ensures all three governance preconditions (CSA, coverage, justification) are checked before any recommendation is generated. Bidirectional reference to `SR_DS_05` completes BP-107. |
| `SR_GOV_75` | --- | governance | Interface inline check: evaluate visibility of a panel, widget, or data element for the current user; suppress UI elements the user is not authorized to view. Invoked by `SR_UI_05` on every render. | Role + compartment evaluator | Inline call from `SR_UI_05` | `UiVisibilityCheck { tenant_id, principal, ui_element_id, context }` | VISIBLE / HIDDEN / READ_ONLY | `UiVisibilityCheck` (JSON) | `UiVisibilityResult { decision }` | Returned to `SR_UI_05`. | Front-end suppression at render time prevents users from being aware of features they cannot use, while back-end checks remain authoritative. Bidirectional reference to `SR_UI_05` completes BP-110. |
| `SR_GOV_76` | --- | governance | Connection Layer inline check: before pulling data from an external system, verify that the connection is approved, the credential is in CaaS, the rate limit budget is available (via `SR_CONN_36` and `REUSABLE_QuotaEnforcer` per `SR_SCALE_25`), and the tenant policy allows the pull. | `connections` table, CaaS health, `REUSABLE_RateLimiter`, `REUSABLE_QuotaEnforcer` | Inline call from Connection Layer pull worker (any `SR_CONN_11`-`SR_CONN_19` adapter) | `ConnectionPullPreflight { tenant_id, connection_id, scope, expected_volume }` | ALLOW / DENY / DEFER | `ConnectionPullPreflight` (JSON) | `ConnectionPullPreflightResult { decision, defer_reason? }` | Returned to Connection Layer worker. | Centralizes pre-pull policy checks; prevents the same checks from being implemented inconsistently across adapters. The `REUSABLE_QuotaEnforcer` reference makes the per-tenant quota gate explicit (BP-128). |
| `SR_GOV_77` | --- | governance | Intelligence Layer query rewrite: every Cypher or vector query is rewritten to inject the tenant_id constraint and the role-based filter (per IL-1) via `SR_DM_27`. Invoked by `SR_INT_23`. | Query rewriter, `REUSABLE_TenantFilter`, `SR_DM_27` | Inline call from `SR_INT_23` (any query path into Neo4j) | `QueryRewriteInput { tenant_id, principal, raw_query }` | Rewritten query with tenant_id constraint and role filter applied; rejected if raw query contains forbidden constructs | `QueryRewriteInput` (JSON) | `QueryRewriteResult { rewritten_query, applied_filters[] }` | Query executor proceeds with rewritten query. | Tenant_id injection at the query rewrite layer is the only sound way to guarantee multi-tenant isolation; any layer that bypasses the rewrite breaks isolation. Bidirectional reference to `SR_INT_23` and `SR_DM_27` completes BP-95. |
| `SR_GOV_78` | --- | governance | Component execution preflight: before any component runs, verify that it is `active`, that its credential requirements are met, that it has not been deprecated past use, and that the executing principal has permission. Invoked by `SR_CAT_30`. | `component_registry`, role evaluator | Inline call from `SR_CAT_30` (component runtime) | `ComponentExecutionPreflight { tenant_id, principal, component_id, args }` | ALLOW / DENY | `ComponentExecutionPreflight` (JSON) | `ComponentExecutionPreflightResult { decision }` | Returned to `SR_CAT_30`. | Centralizes execution gating; prevents accidental execution of deprecated, untested, or unauthorized components. Bidirectional reference to `SR_CAT_30` completes BP-114. |

---

## Back-Propagation Log

This is the first expanded spec, so there are no prior expanded specs to back-propagate to. Future expanded specs will record BP-94 and later entries here that target SR_GOV_ rows.

| BP | Triggered By | Impacted SR | Remediation | Session |
|----|-------------|------------|------------|---------|
| — | First spec — no prior to back-propagate to | — | — | 1 |

---

## Self-Audit Findings

A structured self-audit was performed before declaring this spec complete. Findings and resolutions are recorded below.

| # | Category | Finding | Resolution |
|---|----------|---------|------------|
| 1 | Failsafe coverage | `SR_GOV_16` had no SE coverage for the case where the rule engine returns within SLA but in an inconsistent state. | Confirmed that `SE-01` (engine unavailable) and `SE-02` (timeout) cover the realistic failure modes. An "inconsistent state" failure would manifest as either an exception (SE-01) or a wrong decision; the latter is detected by `SR_GOV_19` dry-run testing on rule publication, which is the correct prevention point. No new SR added. |
| 2 | Admin lockout protection | Confirmed that every SR that could remove admin access (`SR_GOV_11`, `SR_GOV_13`, `SR_GOV_15`) has an explicit BE row preventing zero-admin states. | Verified. Three BE rows: `SR_GOV_11_BE-01`, `SR_GOV_13_BE-01`, `SR_GOV_15_BE-01`. |
| 3 | Cross-store atomicity | `SR_GOV_01` requires both Neo4j and PostgreSQL writes; SE-01 covers failure but the success-path needs to clarify atomicity. | Confirmed: the SR_GOV_01 main row implies atomicity ("creates the Tenant node ... and writes the initial audit record" — both must succeed). The SE-01 row makes explicit that partial state is forbidden. The implementation must use a saga pattern (event-bus + idempotent retries on the failing leg) since true cross-store atomicity is impossible. This is an implementation detail; no new SR. |
| 4 | Break-glass review closure | Both `SR_GOV_29_REVIEW` and `SR_GOV_46_REVIEW` reference a 24-hour SLA but neither has its own SLA-breach handling. | Acknowledged. Adding an inline note: SLA breach on review escalates to platform security officer + tenant security officer. Both review SRs implicitly use `SR_GOV_45` escalation pattern. No new SR; documented here. |
| 5 | Audit chain recovery | `SR_GOV_51` freezes writes on tamper detection but does not specify the manual recovery process. | Acknowledged. Recovery is intentionally manual per the trunk audit model — automated recovery from a tampered chain would be a security weakness. The runbook lives outside the SR row (operations runbook) and is referenced by the alert payload. No new SR. |
| 6 | Compartment access vs ENFORCE | `SR_GOV_33` and `SR_GOV_16` both deny on compartment violation; ensure no double-evaluation or bypass. | Confirmed: `SR_GOV_16` invokes `SR_GOV_33` for any compartment-relevant action; `SR_GOV_33` is the authoritative compartment evaluator. The double layer is intentional for defense-in-depth: a layer that forgets to call `SR_GOV_16` directly is still caught by `SR_GOV_33` at data exposure time. No change. |
| 7 | Justification quality regression | `SR_GOV_18` validates justification quality; but the validator's blocklist may need to evolve over time. | Acknowledged. The blocklist is a `governance_rules` value subject to `SR_GOV_19` versioning. Updates flow through normal rule publication. No new SR. |
| 8 | Coverage of D-29 model whitelist/blacklist | `SR_GOV_66` covers approval but not the per-tenant whitelist enforcement. | Confirmed: per-tenant whitelist/blacklist is enforced inside `SR_LLM_` (Stage 1 router rules R3 and R4 from Spec 05). Governance Layer holds the rules in `governance_rules`; LLM Router enforces them. Cross-reference noted in Section 12 integration map. |
| 9 | IAM polling fallback for missed delete events | `SR_GOV_04` polls for changes since `last_event_at`; verified that it covers user-delete events (not only create/update). | Confirmed: provider SDKs return delete events in poll responses. The mapping logic in `SR_GOV_03` handles all event types identically. |
| 10 | CSA bypass via Decision Support direct query | What if a developer bypasses Decision Support and queries Intelligence Layer directly, escaping CSA? | `SR_GOV_77` enforces query rewrite at the Intelligence Layer, which includes CSA evaluation for any multi-collection query. CSA is therefore evaluated regardless of which API path is used. Cross-cutting protection confirmed. |

**Self-audit conclusion:** All findings either resolved or documented as deliberate. No new architectural decisions required. No gaps to flag for Nick.

---

## Cross-Reference Index

Outbound references from this spec to other specs (these targets must exist when the referenced specs are expanded):

| From | To | Purpose |
|------|----|----|
| `SR_GOV_24` | `CsaAssessment` node in Spec 02 (`SR_DM_`) | Persisted assessment record |
| `SR_GOV_47` | `audit_events` table in Spec 02 (`SR_DM_`) | Audit row storage |
| `SR_GOV_53` | Three-tier NER ensemble (referenced; lives in Spec 04 `SR_INT_`) | Classification engine |
| `SR_GOV_56` | `data_retention_policies` (Spec 02 `SR_DM_`) | Retention policy storage |
| `SR_GOV_57` | `bulk_import_log` (Spec 02 `SR_DM_`) and `SR_CONN_` Type 6 row | Bulk import lifecycle |
| `SR_GOV_61` | `component_registry` (Spec 02 `SR_DM_`) and `SR_CAT_` rows | Component activation |
| `SR_GOV_64` | `service_account_registry` (Spec 02 `SR_DM_`) and `SR_SA_` anomaly rows | SA anomaly response |
| `SR_GOV_66` | `model_registry` (Spec 02 `SR_DM_`) and `SR_LLM_` canary rows | Model swap |
| `SR_GOV_71` | Coverage calculator in Spec 04 `SR_INT_` | Coverage enforcement |
| `SR_GOV_73` | Stage 1 router rules in Spec 05 `SR_LLM_` | Router invocation |
| `SR_GOV_74` | Decision Support generator in Spec 06 `SR_DS_` | Preflight invocation |
| `SR_GOV_75` | Interface render path in Spec 07 `SR_UI_` | UI visibility |
| `SR_GOV_76` | Connection Layer pull worker in Spec 03 `SR_CONN_` | Pull preflight |
| `SR_GOV_77` | Cypher query path in Spec 04 `SR_INT_` | Query rewrite |
| `SR_GOV_78` | Component runtime in Spec 08 `SR_CAT_` | Execution preflight |

These outbound references are tracked in the cross-reference integrity log in `002/STATE.md` and verified during the final regression pass.

---

## Spec 01 Summary

| Metric | Value |
|--------|-------|
| Sections | 12 |
| Main-flow SRs | 78 |
| Exception SRs | 36 |
| Total SR rows | 114 |
| Architectural decisions covered | 25 (D-3, D-17 through D-22, D-29, D-34, D-35, D-37, plus BPs) |
| Trunk capabilities invoked | 14 |
| Reusable components defined | 9 |
| Self-audit findings resolved | 10 |
| New decisions introduced | 0 (architecture locked) |
| BP entries created | 0 (first spec — none to back-propagate to) |

**Status:** Self-audit complete. Ready for downstream specs to reference these SR IDs.

---

## Back-Propagation Received from Later Specs (applied retroactively in Session 2)

These edits were applied AFTER the initial Spec 01 draft to reflect ripple effects from later expanded specs. Each entry references the originating BP number from the originating spec's BP log and the SR row in this spec that was modified. Entries are append-only.

| BP | Originating Spec | Edit Applied to Spec 01 |
|----|-----------------|-------------------------|
| BP-94 | Spec 02 (`SR_DM_05`/`SR_DM_06` audit partition) | `SR_GOV_47` updated to invoke `SR_DM_05` and reference partition strategy `SR_DM_06` |
| BP-96 | Spec 02 (`SR_DM_30` crypto-shred mark) | `SR_GOV_52` updated to explicitly invoke `SR_DM_30` for the marking step |
| BP-95 | Spec 02 (`SR_DM_27` query rewrite) | `SR_GOV_77` updated to reference `SR_DM_27` as the rewrite primitive |
| BP-98 | Spec 03 (`SR_CONN_42` paywalled API) | `SR_GOV_70` updated to accept `paywall_acknowledgement` field in `ConnectionConsentRequest` |
| BP-125 | Spec 03 (`SR_CONN_32` quarantine) | `SR_GOV_56` updated to scan quarantine queue for retention enforcement |
| BP-126 | Spec 04 (`SR_INT_22` aggregate workers) | `SR_GOV_59` updated with bidirectional reference and effective-time semantics |
| BP-102 | Spec 04 (`SR_INT_30` tenant offboarding) | `SR_GOV_52` updated to accept `source: tenant_offboarding` |
| BP-104 | Spec 05 (`SR_LLM_05` Stage 1) | `SR_GOV_73` updated with bidirectional reference |
| BP-127 | Spec 05 (`SR_LLM_50` fine-tune lifecycle) | `SR_GOV_60` updated to reference `SR_LLM_50` and `SR_LLM_51` |
| BP-107 | Spec 06 (`SR_DS_05` decision-support preflight) | `SR_GOV_74` updated with bidirectional reference |
| BP-110 | Spec 07 (`SR_UI_05` visibility check) | `SR_GOV_75` updated with bidirectional reference |
| BP-112 | Spec 07 (`SR_UI_36` offline replay) | `SR_GOV_47` updated to preserve `original_timestamp` metadata |
| BP-113 | Spec 08 (`SR_CAT_15`/`SR_CAT_22`) | `SR_GOV_61` and `SR_GOV_62` updated with bidirectional references |
| BP-114 | Spec 08 (`SR_CAT_30` execution) | `SR_GOV_78` updated with bidirectional reference |
| BP-123 | Spec 12 (`SR_V2_01` field validation) | `SR_GOV_61` updated to include V2 field validation precondition |
| BP-116 | Spec 09 (`SR_SA_30` quarterly review) | `SR_GOV_63` updated with bidirectional reference |
| BP-117 | Spec 09 (`SR_SA_45` auto-suspend, `SR_SA_36`-`SR_SA_43` detectors) | `SR_GOV_64` updated with bidirectional references |
| BP-128 | Spec 13 (`SR_SCALE_25` per-tenant quotas) | `SR_GOV_76` updated to invoke `REUSABLE_QuotaEnforcer` |

**Total retroactive edits to Spec 01 (Session 2): 13 SR row updates across 11 source BPs.**

## Session 3 Additions — Back-Propagation from Workbook

Session 3 cross-verified the expanded specs against `Platform_Requirements_and_Exceptions.xlsx` and applied these Spec 01 additions:

| BP | Originating | Edit Applied to Spec 01 |
|----|-------------|-------------------------|
| BP-133 | Workbook BR_GOV_08 (break-glass 4h/48h defaults) | `SR_GOV_29`, `SR_GOV_29_REVIEW`, `SR_GOV_46`, `SR_GOV_46_REVIEW` updated with explicit 4-hour default token duration and 48-hour review window (accommodates weekends). Resolves the prior 24h review inconsistency. |
| BP-134 | Workbook BR_GOV_05_ValidateJustification_BE-01 (20-char rejection minimum, filler-word blocklist, T1 relevance scoring) | `SR_GOV_72` updated with explicit 20-char minimum, filler-word blocklist, and T1 relevance ≥0.5 threshold. |

**Total Session 3 edits to Spec 01: 5 SR row updates across 2 source BPs.**
